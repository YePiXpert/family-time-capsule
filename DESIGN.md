# 小美成长记 · 纸上日常

本文件是项目的界面设计规范。先读本文，再通过公共主题与组件实现界面。
本轮应用范围：原生首页、记录卡片、公共标题与按钮、导航主操作；其他页面随共享颜色更新。

## 设计来源与取舍

参考 [awesome-design-md 的 Claude 分析](https://github.com/VoltAgent/awesome-design-md/blob/8147538b4226ae41e2487a9179e3bcc1f68e8554/design-md/claude/DESIGN.md)，
取其奶油纸色、陶土色重点和阅读式标题层级，改编为中文家庭成长记录。
参考仓库采用 [MIT 许可](https://github.com/VoltAgent/awesome-design-md/blob/8147538b4226ae41e2487a9179e3bcc1f68e8554/LICENSE)，其文档是第三方设计分析。
本项目的产品名称、图标、插画和家庭内容保持自己的身份；不用参考品牌标志、商用字体或官网营销版式。

## 氛围

像一本随时可以翻开的家庭手记。温暖、清楚、从容。
照片和真实文字居于视觉中心；界面只负责组织内容、说明保存状态、提供下一步操作。
单页主操作以陶土实色强调，其他工具用细边线和低饱和底色。

## 颜色与实现

唯一颜色来源：`mobile/src/design/tokens.ts`。原生组件通过 `useColorTheme()` 获取当前主题。

| 用途 | 浅色 | 深色 |
| --- | --- | --- |
| 页面纸色 paper | `#FAF9F5` | `#201E1A` |
| 内容卡片 card | `#FFFEFA` | `#292621` |
| 主文字 ink | `#28251F` | `#F2EEE5` |
| 次文字 muted | `#6F6A60` | `#BFB6AA` |
| 分隔线 line | `#E6DFD4` | `#474037` |
| 主操作 coral | `#A65D43` | `#E4A084` |
| 主操作文字 onCoral | `#FFFFFF` | `#2A1512` |

参考的陶土色经过加深，保证小字号按钮文字对比度。正文及按钮文字对比度至少 4.5:1。
本机保存成功用 sage；错误和需要处理的警告使用独立语义色。常规待同步状态属于保存提示。
禁止用色彩独自表达状态，同时提供文字或图标。

## 排版

- 首页与标签页大标题 32，常规字重，iOS 使用系统 Songti SC；Android 使用系统 serif 及其中文回退。
- 记录标题 22，常规字重；正文使用系统无衬线字体 16，长段落保持舒展行距。
- 页面说明 15，工具标签 14，日期与状态 12–13。次要信息使用 muted，避免每行都加粗。
- 字体名称与尺度统一放在 tokens；不依赖联网加载字体。平台字形差异可以接受。
- 保留系统字号缩放和应用简洁大字模式。文字容器可以增高，多行按钮可换行。
- 长标题正常换行；卡片摘要最多两行标题、三行正文，完整内容在详情页可读。

## 组件

- **主按钮**：陶土底，48 以上触控高度，600 字重，12 圆角，明确动作文字。
- **次按钮**：纸白底与细描边，不与主操作争夺注意力。
- **筛选**：44 以上高度，10 圆角；选中使用浅陶土底与明确选中状态。
- **图标工具**：44 × 44，提供读屏名称；工具行空间不足时整组换行。
- **记录卡片**：不透明纸色、18 圆角、细边线、20 内边距。照片保留内容比例。
  文字记录直接展示日期和内容；有素材但没有封面时显示小型素材提示，不伪造照片。
  本机保存状态放在内容之后，以细线分隔。
- **大标题区**：一句轻量眉题、阅读式标题、短说明，底部细线；已有插画仅作小型点缀。
- **底部导航**：保留系统玻璃材质和减少透明度回退，降低阴影强度。
  浮动“记录一刻”使用实色主按钮，避免与导航混淆。
- **恢复页**：优先显示下一步操作和资料保留说明；诊断细节由用户展开查看。

## 手机布局与可访问性

页面边距 20，基础间距 4/8/16/20/28。内容卡片贯穿可用宽度，长列表依旧虚拟化。
避免固定文字容器高度；大字模式和窄屏允许工具换行。遵守安全区域及底部导航遮挡留白。
保留 VoiceOver/TalkBack 角色、标签、选中与禁用状态，以及减少动画、减少透明度设置。
深色模式使用独立语义色，内容表面不做整页反色。真实照片不染色、不套装饰滤镜。

## 改动与验收

先改 tokens 和基础组件，再应用到页面。已有业务、离线保存、导入与同步行为沿用原有实现。
验收浅色空库、有记录、重启、读取恢复、深色、大字模式，检查实际原生截图。
设计稿或预览仅用于讨论；最终界面以交付提交构建的安装包为准。
