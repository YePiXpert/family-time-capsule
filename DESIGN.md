# 小美成长记 · 安静苔绿

本文件是 Web 与原生客户端的共享设计规范。移动端优先，通过 `mobile/src/design/tokens.ts`、共享主题与组件实现，页面不得另建一套颜色、字号或按钮。

## 设计依据

视觉主参考为 [awesome-design-md 的 Airbnb 分析](https://github.com/VoltAgent/awesome-design-md/blob/main/design-md/airbnb/DESIGN.md)：照片优先、适中的标题、清楚的信息密度和一致的控件。辅助参考为 [Apple 分析](https://github.com/VoltAgent/awesome-design-md/blob/main/design-md/apple/DESIGN.md)：克制装饰、阴影和强调色。这些是第三方网站分析；采用其原则并转化为手机规则，使用系统字体和自己的品牌内容。

操作参考美柚、宝宝记的公开界面：让日期、内容与常用动作直接可见。核心闭环是「记录一刻 → 整理成相册 → 与家人回看」。实际内容和下一步动作比装饰更重要。

## 颜色、字体与尺寸

| 用途 | 浅色 | 深色 |
| --- | --- | --- |
| 页面 paper | `#F7F8F5` | `#171C19` |
| 内容 card | `#FFFFFF` | `#202722` |
| 正文 ink | `#202923` | `#F0F4EF` |
| 辅助 muted | `#616B64` | `#B7C2B8` |
| 分隔 line | `#E1E6DF` | `#3A463D` |
| 强调 coral（保留旧代码键名） | `#426A58` | `#A6CCB5` |
| 强调上的文字 onCoral | `#FFFFFF` | `#173224` |

- 系统无衬线字体。页面标题 24 / 600，记录标题 18 / 600，正文 16，辅助信息 13；不使用宋体、超大营销标题和全页粗体。
- 正文行距约 1.55。保留系统字体缩放与应用大字模式，长标题和按钮允许换行。
- 页面边距 20，基础间距 4 / 8 / 16 / 20 / 28。照片圆角 16，控件圆角 12。
- 主按钮至少高 48，图标触控范围至少 44 × 44。文字对比度至少 4.5:1；错误、警告具有独立语义色和明确文字。
- 主导航和阅读内容使用不透明表面。避免卡片套卡片、玻璃叠层和每项都加阴影；弹层与浮动控件才需要轻微层次。

## 公共组件与状态

- **按钮**：一处任务用一个苔绿主操作，次操作用低强调样式；操作中保留文字与稳定布局，禁用和读屏状态同步。
- **筛选**：选中使用浅绿和明确选中语义。筛选与多选分开，多选勾选不改变列表尺寸。
- **照片与记录**：照片、日期、正文构成层级。真实照片不染色、不裁掉重要内容；完整查看可打开原件。普通同步状态不重复占据卡片，只有待处理错误、补记或审核保留入口。
- **加载**：说明正在加载的对象，保留已加载内容和可操作位置，分页失败有就地重试。
- **空态**：一句说明、一个下一步；无生日、无账号和无网络均不得挡住本机记录。
- **错误**：输入与选择保留，说明保存到了哪里，提供重试或核对版本。不要把仅保存正文或权限失败显示为全部成功。
- **无障碍**：VoiceOver/TalkBack 标签、角色、选中状态；减少动画时去掉非必要动效。深色使用独立语义色。键盘、窄屏和大字下操作可滚动到达。

## 页面与导航

### 记录

底部固定「记录 / 相册 / 我的」，三个主页同一位置放「记一刻」。首页先展示宝宝身份与简短说明，月份、第一次、搜索和选择直接可见，随后展示真实记录。月龄是补充筛选；未设置生日时按日期使用即可。有未完成内容才显示继续入口。

### 统一编辑与阅读

编辑是全屏导航栈页面，隐藏底部主导航。新建、本机补记和服务器记录编辑使用同一界面，并携带明确身份与目标。正文下直接排列「照片 / 拍摄 / 录音」，日期和读者可点；标题、地点、人物放在一处补充信息中，已有值完整保留。

读者「全家 / 仅自己」选后返回，指定成员有完成按钮。读者调整随保存一起提交。返回、侧滑与切后台写入本机，写入失败保留编辑页；录音离开须先保存或明确放弃。保存打开对应记录，继续编辑仍更新同一条。阅读页直接提供「编辑 / 第一次 / 加入相册」，家人追加仍是独立贡献。

### 相册与成长册

相册主页直接混排本机相册、家庭相册与成长册封面，筛选「全部 / 相册 / 成长册」。分页按更新时间稳定归并，关联后的本机与家庭相册只显示一份。

独立选材页固定月份工具、列表与底部数量。跨月、分页、返回后保留勾选与位置。「添加记录 → 勾选 → 加入此相册」三次点击完成添加。新建相册、成长册与预览共用持久选择会话；返回调整内容不丢名称、封面、读者和样式。

成长册封面直接提供「换封面」，从册内可用照片选择。「内容 / 样式 / 导出」并列，清除重复编辑入口。排版与导出仍需要服务器。

### 本机与共享边界

本机相册默认仅在本机，可离线创建、命名、换封面、排序、移除与阅读。首次同步明确目标家庭、相册名称和材料上传范围；联网不等于授权分享。原记录读者不因加入相册扩大。已同步相册支持离线阅读已下载内容、暂存追加；其他远端修改联网处理。待审核、失败、权限变化清晰可见，不静默漏项。

## 验收

按 320 / 390 宽度、浅深色、大字与键盘展开检查首页、编辑、选材、阅读和失败恢复。使用真实原生截图核对布局；预览稿不代替安装包验收。核心流程覆盖离线首次使用、重启恢复、同一记录补素材、跨月多选、同步幂等、冲突权限及长列表完整性。
