# 小美成长记 · 温暖照片风

Android／iOS 以本机记录为核心的应用，AI 为可选联网功能。核心流程：记一刻 → 保存 → 整理相册 → 回看。

## 参考

采用 [awesome-design-md 的 Airbnb 分析](https://github.com/VoltAgent/awesome-design-md/blob/main/design-md/airbnb/DESIGN.md) 中照片优先、适中标题、温和留白和一致控件的原则。这是第三方网站分析，转译为手机界面；不复制品牌字体、红色或商业页面结构。

照片浏览参考 [Google 相册的日期与网格组织](https://blog.google/products-and-platforms/products/photos/redesigned-google-photos/)：以小图网格呈现记录，减少重复日期和大卡片外框，详情页再展示大图和全文。

## 统一规则

主题与公共组件集中在 `mobile/src/local/ui.tsx`。不得在页面另建主题。

| 用途 | 浅色 | 深色 |
| --- | --- | --- |
| 背景 | #FAF5EC | #221C16 |
| 内容 | #FFFFFF | #2C241C |
| 正文 | #3B3129 | #F2E9DC |
| 辅助 | #7A6A58 | #B8A88F |
| 强调 | #B4553C | #E09B76 |
| 分隔 | #EBDFCC | #453A2E |
| 纸面 | #FFFDF8 | #2C241C |
| 纸描边 | #EBDFCC | #453A2E |

系统无衬线字体；标题与记录标题用衬线（iOS Georgia／Android serif），标题 24／600、记录标题 18／600、正文 16、辅助 13，衬线标题带 0.3 字距。支持系统字号与应用大字。页面边距 20；间距 4／8／12／16／20／24／32。照片圆角 12、控件 12、按钮 14。主按钮至少高 48，图标触控至少 44。文本允许换行，键盘下主要动作仍可用。

采用温润手账视觉语言：页面底层为暖米纸色，铺极淡的静态暖色氛围光斑（由杏色／珊瑚／米黄派生的径向渐变，无动画）；其上是实色纸卡片——卡片、输入框、底部导航、页头、浮动按钮统一走 `Glass` 基元（实色纸面 + 暖色细描边 + 低透明暖棕柔和阴影），呈现纸张层叠感。在 iOS 26+ 且未开启系统「降低透明度」时，`Glass`／`Card`／`BottomBar` 基元渲染为系统液态玻璃（regular／clear 质感，强调处以赤陶 tint 淡染）；Android 与降级路径保持实色纸卡片。页头两个平台都是实色纸面（取纸色而非卡片白，与页面底色连成一片），iOS 去掉分隔线、Android 保留暖色细描边——导航栈的页头外观由 react-native-screens 固定为不透明或全透明二选一，拿不到系统材质，因此不走液态玻璃。两种质感共用同一套间距、字号与圆角。强调色为赤陶（深色为杏色），主按钮实底白字。照片仍是内容主角：照片本身不加边框与装饰，仅圆角；列表与封面渲染持久缩略图，阅读与缩放用原图，按素材真实宽高比呈现，未知比例退回默认。扉页与空态可用 `Ornament` 基元（居中细线加小菱点）收尾。文字对比度不低于 4.5:1。页面转场默认淡入淡出，进入与按压动画克制；尊重系统减少动画设置，开启时全部取消。

## 页面与操作

- 应用常态无底部导航：书架即首页。「记一刻」是书架与月册内页右下的赤陶圆形悬浮钮（56）；设置整体收进书架右上角的印章图标。编辑与选材隐藏悬浮钮。
- 书架：衬线宝宝标题（点击翻开扉页）、搜索与设置图标入口、「N 册 · N 段时光」一行；填了生日时标题下出现年龄行（「2 岁 3 个月 · 来到世界第 825 天」）；出生当天、第 100 天与每周岁当天出现印章式里程碑卡（双线圆环 + 数字，点击直接记一刻）；存在往年同月同日记录时顶部出现「那年今日」横滑卡片条，多条全部可翻；有记录且从未导出或超 30 天未导出备份时，顶部出现温和的备份提醒纸卡（不弹窗、可常驻，附「去备份」入口，导出后自动消失）；距上次记录满 3 天或草稿搁置 3 天时，出现记录节奏提示纸卡（「有 N 天没记啦」／「有一份草稿还没写完」，可关、当天不再出现）；草稿合并为一条入口，可展开其他草稿。年度册区按年倒序排列每一年（封面取当年第一张图，无图用印章圆环加年份数字的纸封面，标注时光数与「第一次」数）；月度册按时间倒序排列（封面取该月第一张图，无图用纸封面，封面左侧带赤陶书脊细条），有「第一次」记录时最前出现「第一次合集」；专题册区排列相册与「新建相册」虚位册；时光系列区排列各系列（封面取最新一张，无图纸封面配「N 个月」印章）与「新建时光系列」虚位册。手机双列、大字单列。书册与悬浮钮按下弹簧缩放、进场交错淡入。
- 搜索：全库按标题、正文、地点不区分大小写检索；筛选 chips（年份、人物、第一次、有声像、纯文字）可叠加；结果为日期倒序的整行记录卡，点击直达阅读。
- 应用锁：开启后冷启动与回前台的解锁门用同一套纸面语汇（小标题、说明一句、主按钮），不展示任何记录内容；退后台即上锁；设备无任何锁屏验证时不锁死。
- 扉页：居中双线印章圆（名字首字）、衬线名字、生日一行（可选）、Ornament 装饰、完善资料入口。
- 月册内页：衬线月份标题、可展开本册搜索；按日期分区、手机默认双列照片网格、平板三列，大字体切换为单列列表。日期集中在分区标题，配强调色小竖条；每条记录只保留短标题和简述。
- 第一次合集：全部「第一次」按日期升序的纸卡列表。
- 年度册内页：衬线年份标题与当年统计一行；有人物时提供人物筛选 chips（过滤「第一次」清单与月册，统计与重放仍是整年）；「这一年回顾」入口进入年度回顾页（印章年份、统计、月度封面横排、第一次清单、寄语与 Ornament 收尾）；「重放这一年」进入全屏重放（照片整屏淡入淡出、4 秒或点击前进、底部进度点、末页收统计与寄语，尊重减少动画，无照片年份禁用入口；默认无声，可从本机已有录音里挑一段循环配乐，选乐浮层打开时暂停自动前进，末页不再显示选乐入口）；「导出成长册」把这一年装订起来，先问一句要长图还是可打印 PDF（见下）；「爸爸妈妈的话」纸卡（年度寄语，最多 2000 字，可编辑，可让 AI 起草一版纯文字草稿）；「这一年的第一次」按日期升序纸卡列表；当年月册小册，点击进对应月册。
- 时光系列内页：衬线系列名与统计一行（张数、跨月数）；时间线按月倒序，已有的月份是月份标签 + 照片 + 记录标题（点击直达阅读），缺口月份是「还缺这一张」纸卡附「把这个月的照片加进来」（弹出该月照片平铺选择，同月后选替换先确认）；整理态可改名、移出单张、删除系列；满两张可导出横排对比卡。
- 相册内页：封面、名称与数量，扉页「这本相册的话」寄语纸卡（与年度寄语同款），记录直接阅读；无类型中转页。
- 编辑：正文与素材优先；照片／拍摄／录音直接可点；日期就地修改，标题地点与人物 chips（多选、就地新建）在一处补充信息；新草稿正文为空时，输入框下出现一行「今天的小问题」温和提示（按月龄分段的中文小问题，同日稳定，「换一个」换种子、「不问了」当次会话关闭，无设置项不落库）。
- 阅读：日期配强调色小竖条，带人物的记录在日期行下以辅助色小 chips 列出人物名；照片、完整文字在下；编辑、第一次、加入相册、做成纪念卡直接可见；同一记录多张照片可上一张/下一张连翻；地点为照片坐标或为空且照片带定位时，可按需换成本地地名（确认后写入）；删除入口弱化为辅助色文字按钮。
- 纪念卡：把一条记录渲染成可分享的纸面卡片图（750 宽）：暖纸底、细描边内框、顶部日期与装饰线、照片圆角裁切、衬线标题与正文、底部装饰线与「某某的成长记」落款；本机生成，不走网络。时光系列对比卡同语汇：横排最多 4 格照片（超限均匀抽样保留首尾）、每格下方月份标签、固定版面与落款。年度成长册同语汇但是竖向长卷（750 宽）：印章年份封面、统计一行、寄语、十二月四列网格（每格月份标签与当月封面照）、「第一次」清单最多 8 条、落款；无记录的年份禁用入口，超长内容从版面上限处截断。同一张长卷另可按 A4 比例分页导出为 PDF，版面不变，只是切页。
- 相册内页：封面、名称与数量，记录直接阅读；无类型中转页。
- 选材：固定月份工具、人物筛选 chips、稳定素材列表与底部数量；勾选不改变布局。
- 整理人物：编辑器人物 chips 下方的「整理人物」入口；按名字排列全部人物，每人显示被多少条记录与草稿用到；可就地改名（trim 后 1–50 字）、合并进另一个人、删除。合并与删除先确认，且只动标记不动记录——标记会从记录、草稿正文与「按事情分组」的每件事上一并剥离。
- 我的（印章进入）：宝宝资料、本机存储（含「本机健康」只读区块：启动耗时、写库平均/最长耗时、写盘失败摘要、最近备份时间，注明只留本机）、备份恢复、外观，分组直接打开。恢复过程在原来那行状态文字上逐步播报（备份当前内容、校验并解包、重建缩略图 n/N、写入本机资料），不新开控件也不弹窗。
- 足迹：书架时光系列区下的「足迹」入口（有带位置照片才出现）；地点卡按照片数倒序——封面照、张数、首末拍摄日期与中心坐标（四位小数），点开列出该地点照片所在的记录；页尾注明位置只来自拍摄信息、始终在本机；空态一句说明与下一步。

## 状态与验收

首次仅简短介绍和「开始记录」。不设置生日也能使用。保存成功表示本机持久写入完成。错误保留输入、说明问题并提供重试。空态一句说明与一个明确下一步。普通记录流程不出现服务器、上传队列或同步占位入口。AI 设置提供邀请加入和主人管理，固定 DeepSeek Flash High。编辑工具栏只有一个 AI 图标（图标 22、触控至少 44），点开共享主题的底部面板：「写记录」提供「AI 生成」「润色我的文字」，「整理照片」提供「按事情分组」；缺少照片或原文时在面板内说明原因，不提供模型选择或自填提示词。润色正文一次最多 2000 字，超限明确提示且不截断；生成时超出单次上限只发送前一段并说明，本机内容不变。分组预览展示每件事包含的照片与画面摘要，并可调整照片归属，确认后每件事都写回草稿；生成与润色结果先预览，润色附原文对照，采用全部或只采用标题、正文后才写入草稿，采用前不覆盖原文；内容修改后旧建议过期并拒绝覆盖。收起面板保留任务与结果，AI 图标只对未被查看的结果显示小圆点，多件事时先选择处理对象；只有通过本机校验后的失败才提供重试，参数错误直接说明原因。断网不影响本机记录。

以真实原生截图审阅首页、编辑、选材、相册、恢复失败状态，覆盖 320／390 宽度、浅深色、大字、键盘、读屏。预览稿不能替代安装包验收。
