# 桉桉成长记 · 手机设计规范

定位以 [PRODUCT](PRODUCT.md) 为准：文字与声音优先，照片作插图。本页记录当前界面约束，不保留已撤回的页面方案。

## 主题与组件

所有主题和公共控件来自 `mobile/src/local/ui.tsx`，页面不另建主题。

| 用途 | 浅色 | 深色 |
| --- | --- | --- |
| 背景 | #FAF5EC | #221C16 |
| 内容 | #FFFFFF | #2C241C |
| 正文 | #3B3129 | #F2E9DC |
| 辅助 | #7A6A58 | #B8A88F |
| 强调 | #B2543B | #E09B76 |
| 分隔 | #EBDFCC | #453A2E |

- 系统无衬线正文；标题与记录标题用 Georgia／serif。标题 24／600，记录标题 18／600，正文 16，辅助 13；支持系统字号与应用大字。
- 页面边距 20，间距使用 4／8／12／16／20／24／32。照片和控件圆角 12，按钮 14。主按钮至少高 48，图标触控至少 44；文字对比至少 4.5:1。
- 暖米纸底与淡静态光斑，实色纸卡、暖描边和柔和投影。iOS 支持时由共享 `Glass` 基元使用液态玻璃；Android 与降低透明度时退回纸面。
- 玻璃及祖先不能设低于 1 的 opacity；禁用／按压透明度落在内部内容。卡不套卡，`GlassDepth` 保证卡内按钮退回实色。横向条给卡片投影留空间。
- 按钮四级：主按钮、次按钮、文字按钮、危险文字按钮。每页至多一个主按钮；危险动作清楚说明后果。页尾删除用 `DangerCard`，编辑页放弃用顶栏垃圾桶。
- 纸卡内短字段用 `FieldRow`，功能入口用 `SettingsRow`／`IconTile`，媒体工具用 `ToolButton`。选中的 chips 带勾。
- 照片无装饰边框，列表与封面用持久缩略图，阅读缩放用原图。应用图标是赤陶底、纸色双圆章与衬线「桉」，源文件在 `mobile/assets/`。

## 页面、键盘与无障碍

`Page` 绘制安全区内的 52 高顶栏，左侧 44 返回钮，中间可换行标题，右侧一个动作。原生导航页头隐藏；表单标题在顶栏，阅读标题随内容。iOS 侧滑与 Android 返回均须处理未保存状态。

内容短时不滚动、不回弹，放不下才滚动。文本允许换行；小屏、大字、键盘弹起时仍可执行主要动作。自带列表补 `keyboardShouldPersistTaps="handled"` 与键盘 inset。页内顶栏下的 `KeyboardAvoidingView` 不重复加顶栏偏移；弹层显式处理 Safe Area。

设置行标签稳定、副题作为 accessibilityValue；异步结果在页内播报。首次录音／相机权限失败给出下一步，输入和已有内容保留。

## 动效与转场

纸面负责阅读，液态玻璃负责操作，转场连接两者。时长与弹簧集中在 `ui.tsx` 的 `MOTION`，是项目自定的起点，不是系统规格；依据与待测项见 [PLAN-IOS-MOTION](docs/plans/PLAN-IOS-MOTION.md)。

- 页面一律平台默认的层级推进（原生 push），写记录、写信同样留在栈里，不用原生 modal／formSheet：它们画在应用锁与多任务遮罩之上。不加整页淡入或第二套 Reanimated 进场。
- iOS 只留左边缘侧滑返回，不开整屏返回，免得抢看原图、横向书架条与最近卡、正文拖选。侧滑与所有返回走编辑页现有的退出守卫，不另建状态机。
- 玻璃按压交给系统 `isInteractive`，不再叠缩放；纸面卡片按下轻缩 0.97、松手弹回，点击不等回弹。`liquid` 同时要求编译可用、运行时有玻璃 API、未开「减少透明度」。
- 底部面板用 `SheetModal`：蒙层淡入、面板上移，收放可反向打断，收完才卸下；锁上立即收起，关掉后读屏焦点回到打开它的按钮。收起面板不取消任务。
- 每个 Modal 最后放 `PrivacyCover`：iOS 开应用锁时，多任务界面与后台快照只见纸面。
- 封信成功后落一次印（等转场停稳再播），失败不播，再进同一封信不播。不做循环、漂浮、呼吸光、彩带或 3D 卷纸。
- 「减少动态」实时跟随系统设置：导航、按压、进场、面板和落印都不动，文字、读屏播报与触感照旧。

## 书架与阅读

- 无常驻底部导航。书架即首页，「我的」是右上名字圆章；「记一刻」为书架和月册右下 56 圆形悬浮钮。
- 书架依次为宝宝页头、至多一张提醒、最近时光、可选草稿、册子与底行。正常首屏放下；大字或矮屏才允许纵向滚动。
- 提醒优先级：合并冲突、落款、里程碑、装订、备份、记录节奏。关闭只是暂不提醒，不删内容或冲突留底。
- 最近卡左右翻，只有一条时整宽；多出的高度给文字。卡上日期、标题、正文与落款保持清楚，照片按比例显示。那年今日放在最近记录之前。
- 册子少时也要完整自然，不为一张封面保留空荡横条；空态一句说明和一个下一步。
- 阅读保留正文、声音、日期、人物、落款与原图查看。专题册、年度册、纪念卡、纸书和信保留；已撤回的系列、足迹、长图与重放不再提供入口。

## 记录、信与 AI

保存成功表示本机持久写入完成。空草稿、空信直接退出时静默清理；有输入时保存或放弃应明确，保存中退出不能丢内容。写记录和写信共用主题、键盘和返回规则。

正文、录音和照片围绕记录组织；落款是家人称呼，可设默认并逐段修改。「说一段」留下录音，转写文字可修改；失败不丢录音。信按约定封存，书架没有信时只留轻量入口。

AI 只有一个工具栏入口，打开底部面板。写作面板提供润色和追问，缺原文时在面板内说明。润色一次最多 2000 字，超限不截断；结果先预览、对照、再采用。原文变化后旧建议失效，收起面板保留任务；未读结果用小圆点提示。

手机不设 AI 设置、模型选择和同意弹窗；已加入家庭直接可用，未加入引导到家庭页。模型名只在面板脚注，联网失败用简短说明，不影响本机记录。任务输入以 [AI 说明](docs/AI-PROMPTS.md) 为准。

## 我的、备份与家庭

「我的」一张设置卡收五行：我的落款、备份与恢复、本机存储、外观、家庭与设备。顶部为宝宝资料与圆章，页尾为应用名与「入淮清洛渐漫漫」。本名与名字来历进入扉页。

备份页按完整备份、已有本机备份、开放归档、家人一起写组织。导出／恢复进度、停止与错误留在原位置；分卷全部选齐后恢复。完整备份主按钮，归档和同步用次按钮，不堆叠多个主操作。本机恢复、导出与同步共用互斥状态。

家庭页区分未加入、开始家庭、出示二维码、管理者扫码批准、已获准、首次同步、恢复与设备管理：

- 开家庭先准备恢复码、抄纸、核对三个词，再提交创建。提交失败留在核对页，可用刚抄下的恢复码找回。
- 加手机通过当面二维码。获准与首次同步分开；本机已有记录时，说明合并并先做本机备份。
- 恢复码只在生成时显示，不提供再次查看入口。输入恢复码用于找回管理者身份，不作为普通加入方式。
- 设备列表显示最后使用、待确认与停用状态。最后一台有效管理者设备不能退出；服务拒绝时保留钥匙和凭据。
- 退出前说明本机内容保留、只撤下这台设备的远端清单。服务确认退出后清除本机家庭凭据。
- 备份页「家人一起写」只管同步、验证远端与冲突查看；家庭身份、恢复码和退出统一去家庭页。清空远端不忘掉家庭钥匙。

已加入且启用自动同步时，回到应用及保存后 30 秒自动同步。暂时网络失败下次再试，不弹窗；其他错误在卡内说明。同步时显示进度、禁用冲突操作，停止可用。

冲突页按时间倒序展示留下的一版与另一版，含落款、时间和全文。「用这一版」保存成新版本，「知道了」只移除留底；失败保留原状。没有主按钮，不再弹确认。

## 文案与验收

写给她用「你」，写关于她用「她」。数「N 段时光」，附件按照片／录音计；混合时称附件。「素材、校验、清单、文件扩展名」仅用于脚注或技术错误，普通记录不出现服务器和上传队列。省略号统一「…」。

图标含义固定：sparkle 是 AI，quote 是她说的话，star 是第一次，heart 是纪念卡与寄语，calendar 是日期，pin 是地点，archive 是备份。

用原生截图与真机检查 320／390 宽、浅深色、大字、键盘、读屏、错误和稀疏资料。效果图不能替代安装包验收；双机和恢复见 [验收清单](docs/ACCEPTANCE.md)。
