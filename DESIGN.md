# 桉桉成长记 · 温暖照片风

Android／iOS 以本机记录为核心的应用，AI 为可选联网功能。核心流程：记一刻 → 保存 → 整理相册 → 回看。

## 参考

采用 [awesome-design-md 的 Airbnb 分析](https://github.com/VoltAgent/awesome-design-md/blob/main/design-md/airbnb/DESIGN.md) 中照片优先、适中标题、温和留白和一致控件的原则。这是第三方网站分析，转译为手机界面；不复制品牌字体、红色或商业页面结构。

照片浏览参考 [Google 相册的日期与网格组织](https://blog.google/products-and-platforms/products/photos/redesigned-google-photos/)：以小图网格呈现记录，减少重复日期和大卡片外框，详情页再展示大图和全文。

## 统一规则

主题与公共组件集中在 `mobile/src/local/ui.tsx`。不得在页面另建主题。

| 用途 | 浅色 | 深色 |
| --- | --- | --- |
| 背景 | #FAF5EC | #221C16 |
| 内容 | #FFFFFF | #2C241C |
| 正文 | #3B3129 | #F2E9DC |
| 辅助 | #7A6A58 | #B8A88F |
| 强调 | #B4553C | #E09B76 |
| 分隔 | #EBDFCC | #453A2E |
| 纸面 | #FFFDF8 | #2C241C |
| 纸描边 | #EBDFCC | #453A2E |

系统无衬线字体；标题与记录标题用衬线（iOS Georgia／Android serif），标题 24／600、记录标题 18／600、正文 16、辅助 13，衬线标题带 0.3 字距。支持系统字号与应用大字。页面边距 20；间距 4／8／12／16／20／24／32。照片圆角 12、控件 12、按钮 14。主按钮至少高 48，图标触控至少 44。文本允许换行，键盘下主要动作仍可用。

采用温润手账视觉语言：页面底层为暖米纸色，铺极淡的静态暖色氛围光斑（由杏色／珊瑚／米黄派生的径向渐变，无动画）；其上是实色纸卡片——卡片、输入框、底部导航、页头、浮动按钮统一走 `Glass` 基元（实色纸面 + 暖色细描边 + 低透明暖棕柔和阴影），呈现纸张层叠感。在 iOS 26+ 且未开启系统「降低透明度」时，`Glass`／`Card`／`BottomBar` 基元渲染为系统液态玻璃（regular／clear 质感，强调处以赤陶 tint 淡染）；Android 与降级路径保持实色纸卡片。页头不用导航栈的原生页头（`headerShown: false`），统一由 `Page` 基元在安全区顶部绘制一行 52 的页内顶栏：左侧 44 的「返回」图标钮（`arrow-left`）、中间衬线标题 22／600（可换行）、右侧一个动作插槽；表单与设置类页面（编辑、写信、我的、备份与恢复、外观、AI 设置、搜索、选材）标题在顶栏，内容类页面（阅读、相册、年度册、系列、读信）顶栏只有返回、标题随内容；首页与弹层不画返回。顶栏与页面同为纸色、无分隔线、不走液态玻璃；iOS 侧滑返回与 Android 硬件返回照旧。两种质感共用同一套间距、字号与圆角。强调色为赤陶（深色为杏色），主按钮实底白字。照片仍是内容主角：照片本身不加边框与装饰，仅圆角；列表与封面渲染持久缩略图，阅读与缩放用原图，按素材真实宽高比呈现，未知比例退回默认。扉页与空态可用 `Ornament` 基元（居中细线加小菱点）收尾。文字对比度不低于 4.5:1。页面转场默认淡入淡出，进入与按压动画克制；尊重系统减少动画设置，开启时全部取消。

控件分四级：主按钮（实底、每页至多一个）、次按钮（纸面胶囊）、文字按钮（辅助色文字、44 的触控高，用于新建、更多、另存、停止等辅助动作）、危险文字按钮（错误色，用于删除、放弃、关闭）。开关类按钮（第一次、她说的话、主题、筛选 chips）选中时前置勾。禁用态统一 40% 透明，不再泛粉。卡不套卡、玻璃不套玻璃：`Card` 与 `BottomBar` 内部的 `Glass`（含按钮）经 `GlassDepth` 上下文退回实色纸面平胶囊。图标一义：`sparkle` 只表示 AI，`quote` 表示「她说的话」，`heart` 只表示纪念卡与寄语，`calendar` 表示那年今日，`pin` 表示地点。

称呼与单位：写给她的（信、寄语的正文与占位）用「你」，写关于她的（记录、提示、空态）用「她」；记录一律数成「N 段时光」，附件按种类「N 张照片／N 段录音」，混合时「N 个附件」；「素材／校验／清单／.xmb」这类词不进正文，只准出现在脚注与恢复失败的技术提示里；省略号一律「…」。图标一义：`sparkle` 只表示 AI，`quote` 表示「她说的话」，`star` 表示「第一次」，`heart` 只出现在纪念卡与寄语卡，`calendar` 表示日期与那年今日，`pin` 表示地点。

## 页面与操作

- 应用常态无底部导航：书架即首页。「记一刻」是书架与月册内页右下的赤陶圆形悬浮钮（56，笔形图标）；「我的」收进书架右上角的名字圆章（名字首字的小印章，读屏标签「我的」）。编辑与选材隐藏悬浮钮。
- 书架：衬线宝宝标题带小箭头（点击翻开扉页）、搜索与「我的」入口、标题下一行元信息（填了生日是年龄行「2 岁 3 个月 · 来到世界第 825 天」，否则「N 段时光」）；提醒卡同屏至多一张，按里程碑 > 装订 > 备份 > 节奏挑一张，都带 ✕：出生当天、第 100 天与每周岁当天的印章式里程碑卡（双线圆环 + 数字，「记一刻」）；一二月里去年有记录且还没装订过纪念册 PDF 时的「去年的纪念册可以装订了」（「去年度册」入口，装订过一次即不再提示）；有记录且从未导出或超 30 天未导出备份时的备份提醒（「去备份」入口，导出后自动消失）；距上次记录满 3 天或草稿搁置 3 天时的记录节奏提示（「有 N 天没记啦」／「有一份草稿还没写完」）。关掉后里程碑与节奏当天不再出现、备份 7 天、装订本季，关闭时间记在库里（`nudgeClosedAt`）。存在往年同月同日记录时出现「那年今日」横滑卡片条（是内容不是催促，多条全部可翻）。区块之间 24 的间距、区标题到内容 8；只有两种区块横着摆封面，其余都是「书册行」，所以只有一段时光时首页也就一屏，不会出现一列孤零零靠左的封面。「最近」（最近 10 段时光的翻页条：整宽时光卡左右翻、按卡吸附，卡宽 = 可用宽 − 56、右侧露出下一张 24 提示还能翻；有图的卡顶是 4:3 铺满的照片，下面 12 号日期、衬线标题与正文两行，无图的卡是纸面上正文四行 + 日期；多于一张时下面一排圆点、当前一枚拉长成 16；区标题右侧文字级「随便翻翻」（不少于 3 段时光才出现）随机打开一段时光的阅读页；空库时换成一张「把今天的小事留下来」纸卡与主按钮「记一刻」）；草稿合并为「最近」下面一张紧凑纸卡（标题一行 + 文字级「继续编辑」，多份可展开）；「那年今日」（有往年同月同日记录时，横滑卡片条）；然后年份就是书架：最近 6 个月按年归组，每个年份一个区块——衬线年份标题「2026 年」、下面一行「N 段时光 · N 个第一次」、右侧文字级「翻开年度册」进年度册页（不再单独摆年度册封面，它和月册封面是同一张照片），名下是该年月册的横向封面条（封面固定 4:3 裁切、宽 140、大字 200，条内间距 12、两侧出血，向右滑；封面取该月第一张图，无图用纸封面、左侧带赤陶书脊细条；封面下书名衬线一行、说明一行）；不在这 6 个月里的更早年份收成「往年」书册行（封面取当年第一张图）。书册行是 `SettingsRow` 的书册形态：60×45 的小封面（照片缩略图，圆角 8；没有照片就是纸面小签——纸底、细描边、左侧书脊细条，中间一个图标或一枚小印章）+ 衬线书名 + 一行说明 + 右箭头，几本一组放在一张纸卡里、行间只有细线；区标题右侧是文字级的新建入口，空区只放一行说明、不摆「+」虚位册：「合集」（有才出现：第一次合集（星）、她说的话（「语」字小印）、足迹（地点针））；「专题册」（相册，标题右侧「新建相册」）；「时间胶囊」（写给多年后的信，落款首字小印，说明「封存至 …」／「还没封存的草稿」／「可以拆了」／「已拆封」，草稿点开进编辑页，其余进阅读页；标题右侧「写一封信」）；「时光系列」（封面取最新一张，说明「N 张 · 跨 N 个月」；标题右侧「新建系列」）。书册与悬浮钮按下弹簧缩放、进场交错淡入。
- 搜索：全库按标题、正文、地点不区分大小写检索；筛选 chips（年份、人物、第一次、她说的话、有声像、纯文字）可叠加；结果为日期倒序的整行记录卡，点击直达阅读。
- 应用锁：开启后冷启动与回前台的解锁门用同一套纸面语汇（小标题、说明一句、主按钮），不展示任何记录内容；退后台即上锁；设备无任何锁屏验证时不锁死。
- 扉页：居中双线印章圆（名字首字）、衬线名字、生日一行（可选）、Ornament 装饰、完善资料入口。
- 月册内页：衬线月份标题、可展开本册搜索；按日期分区、手机默认双列照片网格、平板三列，大字体切换为单列列表。日期集中在分区标题，配强调色小竖条；每条记录只保留短标题和简述。
- 第一次合集：全部「第一次」按日期升序的纸卡列表。
- 她说的话（语录）：阅读页点亮「她说的话」的记录，按日期升序的纸卡列表——衬线大字引正文首段，下方日期与当时月龄；点击进阅读页。空态一句说明。
- 年度册内页：衬线年份标题与当年统计一行（时光、照片、影音、第一次；不再数字数）；一行紧凑次级动作：「这一年回顾」进入年度回顾页（印章年份、统计、月度封面横排、第一次清单、寄语与 Ornament 收尾）、「重放这一年」（有照片才出现，没有时一行辅助色说明）进入全屏重放（照片整屏淡入淡出、4 秒或点击前进、底部进度点、末页收统计与寄语，尊重减少动画；默认无声，可从本机已有录音里挑一段循环配乐，选乐浮层打开时暂停自动前进，末页不再显示选乐入口）、「导出成长册」就地展开一张紧凑纸卡：一句说明 + 「长图」「纪念册 PDF」两个次级按钮 + 文字级「选纪念册封面」（弹出当年照片平铺选择，不选就用最新一张，选择记在本机），选定后纸卡收起；点「纪念册 PDF」先进逐页预览浮层（页内顶栏「‹ 翻一遍再装订」、整页按屏幕尺寸渲染、上一页/下一页翻、报总页数与开本，底部「开始装订」），装订过程回到年度册页原地显示「正在装订 N/M 页」纸卡与「停止装订」，完成后弹系统分享；若有照片的原图撑不满版位，下面补一行清晰度提醒；有人物时提供人物筛选 chips（过滤「第一次」清单与月册，统计与重放仍是整年）；然后依次是「这一年的月册」正方形封面网格（点击进对应月册）、「这一年的第一次」按日期升序纸卡列表、页尾「爸爸妈妈的话」纸卡（年度寄语，最多 2000 字，可编辑，可让 AI 起草一版纯文字草稿）。
- 时光系列内页：衬线系列名与统计一行（张数、跨月数）；一行动作：「导出对比卡」次级（满两张可用）、文字级「整理」（展开改名与危险文字级「删除系列」）；时间线按月倒序，已有的月份是月份标签 + 照片 + 记录标题（点击直达阅读），缺口月份是「还缺这一张」纸卡附「把这个月的照片加进来」（弹出该月照片平铺选择，同月后选替换先确认）；整理态可改名、移出单张、删除系列。点「新建系列」时实体就建好了，还叫默认名又没收进照片就离开，静默清掉，不弹确认。
- 相册内页：封面、衬线名称与「N 段时光」，一行动作：「添加记录」主按钮、文字级「换封面」（有照片才出现）与「整理」（展开改名与危险文字级「删除相册」，列表里出现上移／下移／移出）；记录直接阅读；扉页「这本相册的话」寄语纸卡（与年度寄语同款）放在记录之后、页尾；无类型中转页。
- 写信（时间胶囊）：衬线标题「写一封信」与一句说明；标题、正文（多行）、落款三个输入；拆封日期一行就地改（默认 18 岁生日，没填生日则今天起 18 年，只能选明天以后；iOS 滚轮 + 「日期选好了」）；可加一段录音（与编辑页同一套开始／完成／放弃流程，未完成的录音离开前必须结束或放弃）；已加的录音逐条列出可去掉；底部固定栏（与编辑页同款，键盘弹起时仍在键盘上方）放「保存草稿」与主按钮「封存」，封存前弹确认「封存后就不能再改了，到 … 才能拆。」，封存后进阅读页；页尾弱化的「删除这封信」。文字改动防抖落盘，退出时冲刷；什么都没写就离开（含点「保存草稿」），这封空信静默删除，不弹确认；保存进行中按返回会等这一轮完成再放行。
- 读信（时间胶囊）：没到日子时是一只信封——居中大号双线印章（落款首字）、信名、「还没到日子」、「封存至 … · 落款」、写信日期，以及一个辅助色文字按钮「提前拆封」（确认后才拆）；到了日子标题换成「可以拆了」，主按钮「拆开这封信」直接拆；拆开后衬线正文、右下落款、写于／拆于日期与「听录音」入口（进素材页播放）。草稿态直接转到写信页。页尾弱化的「删除这封信」。
- 编辑：顶栏「‹ 记下这一刻／编辑这一刻」，右侧文字级日期胶囊就地改日期；正文框永远在最上，下面一行小字「草稿会自动保留」；新草稿正文为空时，输入框下出现一行「今天的小问题」温和提示（按月龄分段的中文小问题，同日稳定，「换一个」换种子、「不问了」当次会话关闭，无设置项不落库）；然后是 照片／拍摄／录音／AI 一行、素材列表（照片直接可点，可设封面、移除）；新草稿有两份以上素材时出现开关「按拍摄日期分成几件事」（默认关；从其他应用分享进来且照片跨多天时默认开），开启后追加一张纸卡：说明一句 + 每件事一张小卡（「第 N 件事 · N 个附件」、日期、标题、正文、地点），照片下可「调整归属」，手动调整后正文进入各件事、顶部正文框收起为一句说明；标题、地点与人物 chips（多选、就地新建）收在文字级「更多：标题、地点、人物」里；页尾危险文字级「放弃这份草稿」；底栏主按钮「保存这一刻」（分组时「保存为 N 段时光」，保存中「正在保存…」）；保存进行中按返回会等这一轮保存完成再放行。什么都没写（无标题、正文、地点、人物、附件、录音）就返回，草稿静默删除，不留「继续编辑」也不弹确认。
- 阅读：内容在前、动作在后。多张照片横向分页连翻（下方进度点，两侧上一张／下一张图标钮供读屏，点照片看原图），拍摄时间一行辅助色；日期配强调色小竖条，带人物的记录在日期行下以辅助色小 chips 列出人物名；起了标题的记录才有衬线标题（没起的不拿正文首行充数），然后是完整文字、地点；地点为照片坐标或为空且照片带定位时，一枚文字级「把地点换成地名」（图钉图标，确认后写入）；录音／视频／文件是文字级「听录音」「看视频」「打开文件」，不显示文件名；「第一次」「她说的话」是一行开关（选中带勾，图标 star／quote）；底栏三键：「编辑」主按钮、「加入相册」、「纪念卡」（生成中标题变「正在生成…」）；「加入相册」就地展开现有相册 chips（已在其中的置灰带勾）与文字级「新建相册」（直接用这条记录建册并进入相册页），不再绕进选材页；页尾「删除记录」是危险文字按钮。从「随便翻翻」进来时顶栏右侧多一个文字级「再翻一页」，`replace` 到随机另一段（不会连着翻到同一段，返回栈不增长）。
- 纪念卡：把一条记录渲染成可分享的纸面卡片图（750 宽）：暖纸底、细描边内框、顶部日期与装饰线、照片圆角裁切、衬线标题与正文、底部装饰线与「某某的成长记」落款；本机生成，不走网络。时光系列对比卡同语汇：横排最多 4 格照片（超限均匀抽样保留首尾）、每格下方月份标签、固定版面与落款。年度成长册同语汇但是竖向长卷（750 宽）：印章年份封面、统计一行、寄语、十二月四列网格（每格月份标签与当月封面照）、「第一次」清单最多 8 条、落款；无记录的年份禁用入口，超长内容从版面上限处截断。纪念册是另一条路，不是切长图：20×20cm 方形开本、四周 3mm 出血、300 DPI 位图页（2433 见方），封面（满版照，无照片时用印章纸封面）、扉页（印章首字、名字、生日）、按月分章（章名页带「N 段时光 · N 张照片」引子）、每条记录先文字后照片（单张按原比例，多张走两列方格，一页最多四张且不跨页）、「第一次」清单与寄语都不截断、内容页排页码、落款收尾；排版是纯函数 `book.ts`，`BookPage.tsx` 逐页离屏渲染，逐页取图落盘、边写边生成 PDF，照片按版位取图且绝不放大。
- 选材：固定月份工具、人物筛选 chips、稳定素材列表与底部数量；勾选不改变布局。
- 整理人物：编辑器人物 chips 下方的「整理人物」入口；按名字排列全部人物，每人显示被多少段时光与草稿用到；可就地改名（trim 后 1–50 字）、合并进另一个人、删除。合并与删除先确认，且只动标记不动记录——标记会从记录、草稿正文与「按事情分组」的每件事上一并剥离。
- 我的（书架右上角名字圆章进入）：一句 tagline 下是分组的设置行（图标 + 标签 + 副题 + 右箭头，副题读屏时作为值朗读），点行直接打开：「{名字}的资料」（副题生日或「还没填生日」）；「资料」组：「备份与恢复」（副题「上次导出 N 天前」／「今天导出过」／「还没导出过备份」）、「本机存储」（副题「照片和录音占用 N MB」；页内一行统计「N 段时光 · N 本相册 · N 份草稿」与占用、一句卸载提醒、次级按钮「清理没用到的照片和录音（N 个）」，「本机健康」只读区块收在文字级「查看本机健康」后面：启动耗时、写库平均/最长耗时、写盘失败摘要、最近备份时间，注明只留本机）；「家人与 AI」组：「AI 设置」（副题只看本机令牌：已登录／未登录，不联网）；「应用」组：「外观设置」（副题当前主题，开了更大文字时带出；页内主题是一行三个勾选式紧凑按钮「跟随系统／浅色／深色」，「更大文字」与「应用锁」是开关，应用锁说明一句）。备份页是三张纸卡、一页只有一个主按钮。「导出与恢复」：一句说明；主按钮「导出完整备份」与次级「从备份恢复」一行；一行状态文字平时写「上次导出是 N 天前」／「今天导出过」／「还没导出过备份」，导出与恢复的进度和结果都在这一行上逐步播报（整理照片 n/N、备份当前内容、校验并解包、重建缩略图 n/N、写入本机资料），不新开控件也不弹窗；导出时照片先进本机 blob 库、再从库里拼成 .xmb 一卷一卷交给系统分享面板（超过 2 GB 分卷，状态行写「正在写第 1 卷／共 3 卷」「请保存第 1 卷／共 3 卷…」），进行中同一行出现文字级「停止」；「从备份恢复」的文件选择允许多选，分卷要一起选中，缺卷、重复或混选都在状态行用人话说明；12 号脚注说明备份是 .xmb 文件、要保存到应用之外、超过 2 GB 会分卷——`.xmb`、素材、校验这类词只准出现在脚注和读不出文件时的提示里。「本机保留的备份」（有才出现）：每份一行「9月19日 15:44 · 0.1 MB」，不显示文件名（大小是清单加它引用的照片；三份保留备份只占一份照片的空间），下面三个文字级动作「恢复这份备份」「另存」（清单备份同样先拼成 .xmb 再分享）「删除这份备份」（删除是危险色）；恢复前的确认弹窗写明会换成多少段时光、现在的内容会先备份一份。「开放归档」纸卡：一句说明、年份 chips（全部 + 有记录的年份，只有一年时不显示）、「包含未拆封的信」开关（默认关）、次级按钮「导出开放归档」；进行中按钮文字变成「正在归档 n/N 个附件」并在同一行出现「停止」，不弹窗；空间不够或照片缺失在卡内一行红字说明；完成后进入系统分享面板，归档留在应用缓存直到下次导出。「远端备份」纸卡放在开放归档之后（`remote-card`），是远端唯一的入口，三态：未登录只有一句说明与文字级「去登录」（去 AI 设置）；已登录未开启是一句说明、次级「开启远端备份」（生成钥匙后直接进恢复码页）与文字级「从远端恢复」；已开启一行「上次备份 9月20日 · 1.2 GB · 318 份」，次级「现在备份」（进行中标题变成「正在上传 n/N」，同一行出现文字级「停止」），下面一行文字级「查看恢复码」（开了应用锁先过一次生物识别）「验证远端备份」「从远端恢复」与危险文字「关闭远端备份」（弹窗两路：保留远端只关闭／同时删除远端），脚注写钥匙指纹与「恢复码丢了谁也打不开」。错误是卡内一行红字，不弹窗。恢复码页（`RecoveryCode`）：show 态把 12 个词按三列编号摆开、主按钮「我已抄在纸上」；enter 态一个多行输入框、主按钮「开始恢复」、进度与结果在一行状态文字上、进行中有「停止」。恢复码就是钥匙：不进普通记录流程，不进 Library，不写日志。
- 足迹：书架「合集」条里的「足迹」册（有带位置照片才出现）；地点卡按照片数倒序——封面照、张数、首末拍摄日期与中心坐标（四位小数），点开列出该地点照片所在的记录；页尾注明位置只来自拍摄信息、始终在本机；空态一句说明与下一步。

## 状态与验收

首次仅简短介绍和「开始记录」。不设置生日也能使用。保存成功表示本机持久写入完成。错误保留输入、说明问题并提供重试。空态一句说明与一个明确下一步。点开就建的草稿、信、时光系列，什么都没填就退出时静默清理，不弹确认。普通记录流程不出现服务器、上传队列或同步占位入口。AI 设置页首一句「用家人账号登录后，AI 可以帮你整理照片、写记录。原图和记录仍在本机。」，提供账号登录（空服务仅一次创建主人账号，其余家人账号由主人创建）和主人管理，「刷新状态」是文字级；固定 DeepSeek Flash High，模型名只出现在页尾与 AI 面板底部的脚注「由 DeepSeek Flash High 提供」；连不上服务时只说「现在连不上服务，请稍后再试。」。编辑工具栏只有一个 AI 图标（图标 22、触控至少 44），点开共享主题的底部面板：「写记录」提供「AI 生成」「润色我的文字」，「整理照片」提供「按事情分组」；缺少照片或原文时在面板内说明原因，不提供模型选择或自填提示词。润色正文一次最多 2000 字，超限明确提示且不截断；生成时超出单次上限只发送前一段并说明，本机内容不变。分组预览展示每件事包含的照片与画面摘要，并可调整照片归属，确认后每件事都写回草稿；生成与润色结果先预览，润色附原文对照，采用全部或只采用标题、正文后才写入草稿，采用前不覆盖原文；内容修改后旧建议过期并拒绝覆盖。收起面板保留任务与结果，AI 图标只对未被查看的结果显示小圆点，多件事时先选择处理对象；只有通过本机校验后的失败才提供重试，参数错误直接说明原因。断网不影响本机记录。

以真实原生截图审阅首页、编辑、选材、相册、恢复失败状态，覆盖 320／390 宽度、浅深色、大字、键盘、读屏。预览稿不能替代安装包验收。
