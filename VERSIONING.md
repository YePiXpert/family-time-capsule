# 版本与发布顺序

当前产品版本为 **0.0.1**。2026-09-11 按维护者要求重新编号，后续沿 `0.0.x` 维护。
这是产品版本的重新命名：历史发行、数据库迁移和原生构建号继续保留。

`main` 当前移动端迭代为 **构建 39**（新增模块回归、权限及播放修复），产品版本仍为 `0.0.1`。
2026-09-12 按维护者要求恢复测试。成套安装包通过 `mobile-build.yml` 从同一明确的 main 提交构建，下载与验证证据随该次 workflow 保存。
下面的发行身份表描述不变的 `v0.0.1` tag，仍对应构建 35；不覆盖既有发行附件。

## 当前发行身份

| 字段 | 0.0.1 |
| --- | --- |
| Git tag | `v0.0.1` |
| 发布通道 | `development`（GitHub 标记为 prerelease） |
| 维护纪元 | `formal`：当前维护主线的内部标识，不等同于 stable |
| 发布序号 sequence | 18，接续 `1.0.0-dev.3` 的 17 |
| 原生构建号 | Android 35 / iOS 35 |
| 对外展示版本 | Web、移动 package、Expo、运维工具统一为 `0.0.1` |

## 历史与升级

- 2026-08-29 至 2026-09-06 的 `0.1.x`、`1.x-alpha`、`1.0.0-rc.1~4` 属于探索期。
- 随后的 `1.0.0-dev.1~3` 是当前维护主线的早期发行，发布序号为 15–17。
- 新的 `0.0.1` 发布序号为 18，允许从这些旧版本向前升级。
- 历史 tag、Release、附件保持原样，不移动、不覆盖。

标准 SemVer 会把 `0.0.1` 排在上述历史版本之前，因此它不能表达本项目这次重新编号的升级顺序。
**升级顺序的唯一依据是 `scripts/ops/lib/releases.json` 的 sequence，绝不用 `sort -V` 判断。**
从 `0.0.1` 返回旧代码和旧数据须走显式回滚流程，不因版本字符串变小而自动执行。

## 五条独立的版本轴

| 轴 | 规则 |
| --- | --- |
| 产品版本 | 当前 `0.0.1`，后续按维护者指定版本登记 |
| 发布序号 | 全局单调递增，不归零 |
| Android versionCode / iOS buildNumber | 每次交付递增，即使产品版本重新编号也不降低 |
| 数据库迁移编号 | 沿现有迁移链延续，以 migration journal 为准 |
| API / 导出格式版本 | 按协议兼容性演进，独立于产品版本 |

## 发布注册表与通道

注册表是版本身份的唯一可信来源，每项包含 `version / channel / era / sequence / tag / date`。

1. 未登记的版本拒绝安装、部署和升级。
2. 镜像 digest 不携带产品版本，使用固定 digest 时必须传 `--version`。
3. 探索期可以迁入当前维护主线；当前维护主线不能普通升级回探索期。
4. 同一维护主线只能向更高 sequence 前进，等号允许一致性重装。
5. stable 转非 stable 仍须显式 `--allow-nonstable-target`；真正的数据回退使用 `ftc rollback`。

| 通道 | 含义 |
| --- | --- |
| exploration | 历史探索期，只保留升级来源 |
| development | 早期开发发行，包括当前 `0.0.x` 和历史 `1.0.0-dev.N` |
| candidate | 达到候选条件、尚在验收的版本 |
| stable | 通过明确发布门禁后登记；当前 `0.0.1` 不作此承诺 |

`0.0.1` 没有连字符，但仍属于 development；GitHub 是否标记 prerelease **由通道决定**，不由版本字符串猜测。
既有 `docs/release-1.0/` 继续保留需求与真机验收记录，不表示当前发行号仍为 1.0。

## 发布与部署纪律

- 只从 `origin/main` 上经过 CI 核验的提交发布；tag 一经发布不得移动或复用。
- APK、未签名设备 IPA、服务镜像和运维工具对应同一提交；附件附 SHA-256 和 release manifest。
- 运行中的应用版本读取镜像内构建信息；部署来源还应核对镜像 digest 与源码 revision。
- 每次部署先停写、制作一致快照，在新卷上隔离验证后成对切换镜像与数据卷。
- 故障回滚按 [UPGRADE.md](docs/UPGRADE.md) 执行准备、权限/删除对账和启用。
