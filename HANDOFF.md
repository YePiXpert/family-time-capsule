# 当前交付状态

更新：2026-09-26。只记录当前事实与未完成事项；历史改动见 [CHANGELOG](CHANGELOG.md) 和 Git 历史。

## 源码与部署

| 对象 | 当前状态 |
| --- | --- |
| 功能源码 | main 在 1.1.6 之后有「重构第二轮」的手机与服务端改动（`d1295b5`…`c12649a`），**未打包、服务端未部署** |
| 生产服务 | `4f9b079`（`4f9b0791869515a5826d4ada7d5cc25abe2aae18`），2026-09-25 20:33 UTC 部署成功，全局 review 与重构的服务端改动都已生效；部署证据 `/opt/anan-ai/deployments/20260925-refactor/` |
| 文字 AI | `gpt-6-astra`，五种任务统一 `medium` |
| 语音转写 | `mimo-v2.5-asr`，与文字共用私有上游配置 |
| 最新安装包 | **1.1.6 / 88**，源码 `232369982e99eceeeeeb6da12c9c2504e459e921`（全局 review 与重构：同步可靠、更快、更稳） |

源码、服务器和安装包分别交付。1.1.5 / 87 之后的全局 review 与重构都改了手机和服务端：服务端 2026-09-25 20:33 UTC 部署，手机 1.1.6 / 88 20:52 UTC 发布。之后的「重构第二轮」（下一节）两边都改了，还没交付。

## 重构第二轮：万无一失（2026-09-26，未发布）

主人要求「继续重构确保万无一失」。四路独立审计（本机数据层、同步／家庭／加密、服务端、界面流程与 AI）在隔离副本里用失败测试确认问题，修复后又由两位没参与实现的复审各查一遍；复审找出的问题也已修复。每项都有回归测试，去掉修复会失败。全部用合成资料，未调用付费上游，未连生产。

| 等级 | 问题 | 修复 |
| --- | --- | --- |
| P1 | 阅读页取消「第一次」、清掉「她说的话」、正文改回原样：别的手机认成旧版，一直不跟 | 合并先看世系：接着本机改的照收、早已包含的不动；改回去的一版要整条世系接得上才算旧（`b5c1a0e`） |
| P2 | 写信页开着时同步并进了家里对这封信的改动：接着打字、放弃录音都会拿旧字悄悄盖掉，别的手机不出卡 | 世系记编辑页的来源版本；没写新字不落盘；空闲时换成并进来的新版（`d1295b5`、`96eaea4`） |
| P2 | 写信途中这封信在别的手机被删或封存：按返回出不去；清空后返回会替全家删掉别人封好的信 | 没写新字直接退出、写了另存为新信并说明；只删编辑页看到的那一版（`d1295b5`、`96eaea4`） |
| P2 | 编辑途中原记录在别的手机被删：同步已把草稿改成新记录，下一个字又写回旧 id，之后怎么保存都被拒 | 原记录与基准版本归库管，编辑页不写回（`d1295b5`） |
| P2 | 年度／相册寄语写了一半按返回或侧滑：字直接没了 | 先问「继续写／不保存／保存」，保存途中返回等存完再走（`71b2ac4`） |
| P2 | 缺一张照片原件：「恢复前」那份备份失败，任何恢复都做不了 | 那一份跳过缺的素材并说明个数，照样能恢复；自己点的备份仍然缺就报错（`a4b37f4`） |
| P2 | 时钟慢的手机接着快钟手机改的一版被当成旧版出卡；删掉快钟手机改过的东西，别的手机不删 | 后代永远不算旧；墓碑至少晚于被删那一版一毫秒，「用这一版」存在墓碑之后（`b5c1a0e`） |
| P2 | 恢复换库之后才忘合并历史，中间被杀掉就再也并不回备份之后家人的记录 | 换库前先忘，恢复失败原样放回（`74de5ed`、`96eaea4`） |
| P2 | 加入时确认成功、回应丢了：再轮询去领钥匙包得到「已经加入」，退回出码前 | 用存好的令牌再确认，过了二维码时限也照样确认（`74de5ed`、`96eaea4`） |
| P3 | 只装订了年度册、只删了东西（墓碑）：同步认为没变，不发布 | 清单的库根与「发出去的内容」指纹用同一个函数算（`74de5ed`） |
| P3 | 每天的小问题、默认落款、锁与外观、选片、提醒卡等本机设置也发给了家人 | 换成空库默认值再发，旧版校验照过；新加的根字段不分类就编译不过（`74de5ed`） |
| P3 | 书架「写一封信」、阅读页「新建相册」转场中连点：多出一封空信／一本空相册并同步给全家 | 与「记一刻」同一个聚焦守卫（`71b2ac4`） |
| P3 | 界面监听抛错让已落盘的改动报失败，调用方随后删掉库里正引用的照片 | 落盘后的通知出错只记下（`a4b37f4`） |
| P3 | 往西飞、夏令时回拨后，保留备份按名字里的本地时间排，清掉的是较新的一份 | 按文件里的 UTC 创建时刻排（`a4b37f4`） |
| P3 | 截断标题、AI 上下文、落款、人物名会把表情劈成半个字符 | 按码点截（`a4b37f4`） |
| P3 | 「清理没用到的照片和录音」不占本机互斥，可能删掉刚进冲突留底的照片 | 与备份、恢复、同步共用互斥（`a4b37f4`） |
| P3 | 退出家庭：服务端拒绝时清单已撤下；回收失败算整次同步失败 | 退前拿最新设备表核一遍，被拒就马上发回清单；回收失败不算失败（`74de5ed`、`96eaea4`） |
| P3 服务端 | 匿名乱试恢复码灌满限流，管理者对的恢复码也被 429；一个地址能占满全部 20 个配对名额 | 恢复只把核对不过的计数；配对同一地址最多挂 3 条（来源地址只存带键哈希）；可选 `TRUST_PROXY`（`99f69f2`、`81c6669`） |
| 加固 服务端 | ffmpeg 按上传内容猜格式，冒充 ffconcat 能让它读临时目录里别的文件 | 强制 MP4 解复用、只准本地文件（`99f69f2`） |

试过又撤回（`c12649a`）：为了让「名字、人物、格言、头像改回见过的值」也能传开，曾按「各台上次发布的值」判断新改动，并让空合并历史时听家里最新的清单。复审证明三台以上手机会来回翻个不停、按手机时钟判「最新」会把旧值传开，比原来（改回去的那台和别家不一致）更糟，已撤掉，并用端到端测试钉住「三台怎么同步都会停下来」。

已知局限（旧代码同样如此，未改）：
- 名字、格言、头像、年度寄语与封面、人物改回见过的值：新版手机之间已能传开（根值与人物带版本时刻，未发布，见 `docs/ARCHITECTURE.md` 同步一节）；家里还没升级的 1.1.6 手机仍跟不上根值的改回（人物能跟上）。
- 两台手机各自改出完全相同的内容时，世系按内容哈希分不清，偶尔两边不一致；改回去超过八次加上时钟慢也会判错。
- 恢复旧备份后，家里之后清空的格言、头像会在这台手机上保留旧值（新版手机之间已解决：清空带时刻，恢复的旧值没有或时刻更旧）。

**部署注意**：服务端新增 `pair_requests.source` 列（启动时自动加，可回退）；`TRUST_PROXY` 可选，默认不认转发头。不设时经反代进来的连接（回环或私有地址）不按地址分配对名额，只算全服务 20 条，行为与现在相同；要按真实客户端分，在私有 env 设反代地址（见部署指南）。手机端改动不依赖新服务端。

门禁（`npm run check`）：手机 **973**、服务端 **237**、Python **55** 项全部通过，双端类型检查、手机 Lint 和网络边界检查也通过；推送 `c12649a` 前跑过。同步相关套件连跑三遍全过；合并基准与改前持平。

## 全局 review（2026-09-25，已随服务端 4f9b079 与 1.1.6 交付）

文档与代码一起审了一遍。文档方面：旧计划与审查归档到 [docs/history/](docs/history/README.md)，新增[结构与协议](docs/ARCHITECTURE.md)（含 36 条服务端接口），现行文档逐条按代码校正，真机待验项并进[验收清单](docs/ACCEPTANCE.md)。仓库方面：根目录 `npm run check` 跑全部门禁，清掉了模板遗留文件，部署文件里的私有值改为环境变量。

两路独立审计（服务端、手机端家庭／同步／AI 与工具链）都在隔离副本里用失败测试复现问题；确认的问题都已修复，并各带回归测试：

| 等级 | 问题 | 修复 |
| --- | --- | --- |
| P1 | 阅读页标「第一次／她说的话／地点」、删除或合并人物、合并时修引用，改记录时都没带世系。别的手机会因此出假冲突卡，顶掉还没处理的真冲突卡，输的那版文字就此丢失 | 所有改记录和信的路径都带上世系（`713fb6a`） |
| P2 | 重新生成恢复码时先交后显示：服务端已生效但响应丢了，旧码作废，新码也没见到 | 先抄写核对，再提交同一份；服务端把原样重交当作成功（`0e7df57`） |
| P2 | 打 `v*` 标签发版时，签名密钥缺失会退回模板 debug 证书，并照样发布 | 标签构建必须用持有者密钥、只认钉住的指纹；标签还要等于 app.json 的版本（`85201f8`） |
| P3 | 网络边界检查可以被绕过：换名导入、传引用、文件系统下载、src 之外的入口、`import()`／`require()` | 扩大扫描范围和匹配规则，新增 10 条用例（`dfd6b24`） |
| P3 | 对象刚被回收时开始下载，Fastify 回自己的 500，手机报「服务暂时不可用」 | 先打开文件再作答：没了就回 404，已经打开的照样读完（`b9a10ef`） |
| 文案 | 激活提示还写「我的 → 家庭与设备」；`ai status` 和 413 提示还在说照片；提示词底座还写照片与画面 | 已改；提示词与 AI 说明逐字同步（`43f2b0c`） |

核对后不改：
- 1.0.8 手机升级（`upgradeFamily`）也是先交后显示，但生产已经有家庭，这条路不会再走。
- 转写最坏情况是 ffmpeg 30 秒加上游 100 秒，超过手机的 115 秒：手机放弃了，服务端仍计一次额度。只在上传慢、上游也慢时出现。
- 管理者在手机上启用／停用家人时，会连同当前额度一起提交，可能盖掉刚从命令行改的额度。
- 被停用的手机手里已有的钥匙收不回来，这是设计本身（PRODUCT「家庭与设备」）。
- `local_boundary.py` 里有意再写一次服务地址，用来钉住 `brand.ts`，防止地址被悄悄改掉。

门禁（`npm run check`）：手机 **886**、服务端 **220**、Python **55** 项全部通过，双端类型检查、手机 Lint 和网络边界检查也通过；推送 `b9a10ef` 前跑过（之后的数字见下节）。全部用合成资料，未调用付费上游，未连生产。

## 重构：性能与鲁棒（2026-09-25，已随服务端 4f9b079 与 1.1.6 交付）

主人定的范围是「手机＋服务端全部」，重构完一起部署。提交：服务端 `e7b3b1f`、`fcc53db`，手机 `fa96d5c`、`cf55222`＋`b2635dc`、`5a9163e`、`6582217`、`6fceac1`、`c980227`、`acedaa6`。所有数字都在本地用合成资料测得。

| 方面 | 问题 | 修复 |
| --- | --- | --- |
| 同步（缺陷） | 同步判断「和上次一样」时只比较记录，年度寄语、封面、落款格言这些改动永远发不出去 | 比较内容改为实体加共享的资料库根字段（`6582217`），并新增测试 |
| 同步／备份性能 | 解清单时逐个素材查全表，是 O(B×M)；合并时每轮都重算全部哈希；对象 id 每次都重新做 HKDF | 改为一次建表；冻结的实体记住哈希；对象 id 按钥匙记住。一万条：解码 44 秒降到 0.13 秒，上传规划 264 毫秒降到 14 毫秒；按手机没有 JIT 的速度，一千条合并 3 份远端 3.1 秒降到 57 毫秒 |
| 保存 | 保存一条记录也要把全库重新校验一遍 | 只改了草稿或选集时只校验改动的部分（`6fceac1`） |
| 页面 | 书架、年度册、搜索每次渲染都把全库重新排序、分组、转小写 | 按冻结的集合缓存；搜索够 100 条就停（`c980227`） |
| 手机失败路径 | 同步状态或冲突文件重写被打断会忘记家庭或丢掉冲突版本；冲突写失败仍会合并；分享原件永久占空间，空间不足时分享被丢；导出清理失败会锁死备份页；应用锁在系统弹窗出错时会打开；家庭页连点两次抢走「停止」 | 逐条修复，每条都有回归测试（`fa96d5c`、`cf55222`） |
| 服务端 | 匿名大请求体（15 MiB 约占 530 MiB 内存）；关停总被强杀；磁盘满时连读取也失败；热点查询没有索引 | 先验令牌再读请求体，请求体设上限；8 秒优雅关停；磁盘满回 507；补索引（`e7b3b1f`） |
| AI 年度编者 | 一年最多送 400 条，每条光固定字段就约 190 字，一年三百多条时超过服务端 60000 字的上限，只能提示「记录太多」 | 记录改用编号 "1"…"400" 发送、结果换回真 id，正文与标题多两档收紧；最坏情况 400 条 57119 字，服务端原有契约直接接受，不用改服务端 |
| 结构 | `app.ts` 装着全部 36 条路由；`Settings.tsx`、`Shelf.tsx` 过大 | 路由拆到 `server/src/routes/`，匿名路由在定义上标注；手机端拆出 `BackupPages.tsx`、`ShelfCards.tsx`、`Volume.tsx`。只挪了位置，比对确认内容一致 |

核对后不改：
- 事件监听里的异常改用 `setTimeout` 重新抛出：在 RN 正式包里可能直接让应用崩溃。
- 备份与同步合用一把锁：目前没有竞态，合并只会让测试替身大改一遍。
- `model` 和 `ai/state` 之间的循环导入：无害。
- 上传前再算一遍哈希：读写量翻倍，只为防一种很少见的情况。
- `ui.tsx` 拆分：收益低。
- 延迟复制资料库：风险中等，而写入本来就合并延后了。

待主人决定：
- **原生 SHA-256 与加密**：手机上的 JS 实现没有 JIT，比 Node 慢约 100 倍，4 MiB 照片的哈希和加解密是剩下最大的开销。改成原生模块工作量大，建议以后再做。

门禁（`npm run check`）：手机 **918**、服务端 **231**、Python **55** 项全部通过，双端类型检查、手机 Lint 和网络边界检查也通过；推送 `4f9b079` 前跑过。性能基准不进门禁，用法见[开发指南](docs/DEVELOPMENT.md)。

## 1.1.5 冻结前收尾（已交付）

一次性审计记录、复审与证据在 [FINALIZATION](docs/history/FINALIZATION-1.1.5.md)；24 项问题（P1 五项、P2 十八项、加固一项）已修复并各有回归或截图。要点：

- 草稿、草稿独有及已丢弃的照片不再随同步发到家里；清单只带共享内容用到的素材。
- 冲突页与同步互斥；重新获准的手机第一次同步会真正发布；同步卡随同步状态刷新。
- 备份分享面板点「已存好」才记为已备份；恢复可停止、失败不挤掉更早的恢复记录、选择器副本用完即删；已加入家庭时恢复前说明下次同步会并回之后的改动。
- 应用锁在系统锁屏密码关闭时保持上锁并说明下一步（主人决定不自动放行）；寄语 AI 草稿不覆盖未保存的字；搜索超过 100 条时说明。
- 编辑器保存中锁住输入、安卓日期取消不算选择、连点「记一刻」不多出空草稿、可阅读副本日期按本地日。

1.1.5 期间服务端未改（`git diff f0591ec..e25af17 -- server` 为空），生产不需部署。

## 1.1.5 验证证据

1.1.5 全量：手机 **882** 项（含交付后为原先只靠走查的 11 项补的 35 条回归）、服务端 **217** 项测试通过；双端类型检查、手机 Lint、网络边界和 **45** 项 Python 工具测试通过。双机同步 6 条用进程内真服务和两份隔离客户端；异步与取消相关套件连跑 5 次全过；一万条记录打开 59 ms、改一字 12 ms。全部用合成资料，未调用付费上游，未连生产。

未参与实现的独立复审在隔离副本复跑门禁，提出的三项与一项加固已修复（FINALIZATION #21–24）。

## 已交付安装包

[Release 1.1.6](https://github.com/YePiXpert/family-time-capsule/releases/tag/v1.1.6)：标签 `v1.1.6` → `232369982e99eceeeeeb6da12c9c2504e459e921`，来自 [run 36186790573](https://github.com/YePiXpert/family-time-capsule/actions/runs/36186790573)（20:36–20:52 UTC），八个作业（含 GitHub Release）成功。APK、未签名 IPA 与 build-source.json 已下载核对：Release 与 CI 产物逐字节相同，Release 校验和一致；APK（versionName／versionCode）、iOS 主应用及分享扩展均为 1.1.6 / 88。

| 文件 | SHA-256 |
| --- | --- |
| `FamilyTimeCapsule-android.apk` | `2cf952228d68601e4c972ea897dfc7ea41c6fc2c929d0bbfa182da55768bcd5f` |
| `FamilyTimeCapsule-ios-unsigned.ipa` | `533d5a65ed69a36ec6f5c81548cbb4990878bf3e61bcdfa8605df5c7f6317943` |

安卓本机回归（320／390：编辑器与信一屏、离线启动、草稿恢复、备份往返、年度册、封信、可阅读副本分享）、iOS 26.5 启动验证与完整回归（年度册 PDF、开放归档、完整备份恢复、打不开的资料库找回）通过，报告源码均等于安装包完整 SHA。核验摘要：`/var/tmp/anan-1.1.6-verified/verification.json`。

上一版 [Release 1.1.5](https://github.com/YePiXpert/family-time-capsule/releases/tag/v1.1.5)（`e25af17`，run 36107983728）已被取代。

## 剩余事项

1. **重构第二轮未交付**（见上节）：服务端需按部署指南先过 staging 再部署（`TRUST_PROXY` 可不设）；手机下一版是 1.1.7 / 89。待主人决定：根字段与人物要不要加版本，让「改回原值」也能传开。
2. **服务端已部署（2026-09-25，主人授权）**：`4f9b079` 按[部署指南](deploy/README.md)先过 staging（3141 空库，`verify-service.py --skip-text --skip-transcribe` 全过；匿名 5 MiB 请求 3 毫秒回 401，匿名接口 2 MiB 回 413，`docker stop` 退出码 0），再停写、快照数据目录、切换。切换后本机与公网健康均为新版本，匿名接口仍拒绝，家庭／成员／设备／设置／清单逐行不变，四条新索引已建，两个监听地址与切换前一致（私有 env 已补 `APP_BIND`）；旧容器停机 0.5 秒。未调用付费上游。
3. **手机 1.1.6 / 88 已交付**（见上节）。真机上值得先看：年度册「AI 建议目录」（一年记得多也能用）、重新生成恢复码先抄后交、分享进来的照片不再占双份空间。
4. **1.1.5 的收尾改动（备份「已存好」、冲突页、应用锁提示）与 1.1.4 的设置新布局、纸卡外观与 iOS 动效真机手感未验**（模拟器回归与截图已过）。按 [验收清单](docs/ACCEPTANCE.md) 第二部分看。
5. **两台真机完整验收尚未完成**。按 [验收清单](docs/ACCEPTANCE.md) 验扫码、双向声像同步、冲突留底、断网重启和备份换机；用测试资料演练恢复。
6. 完成后持续使用，只修实际问题。公开注册、多租户、多孩子不做；密钥轮换已延期，不重新列为本轮前提。

## 继续工作

先按 [AGENTS](AGENTS.md) 拉取 main、查看状态与起始 CI；开发门禁和易错约束见 [开发指南](docs/DEVELOPMENT.md)，部署见 [部署指南](deploy/README.md)。当前规则仅 Fable 默认不轮询构建；其他模型跟踪待交付构建到结束，日常推送不轮询。
