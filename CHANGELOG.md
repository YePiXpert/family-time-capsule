# Changes

## 1.0.1 — 界面焕新与返回箭头修复（构建号 74）

1.0.0 之后不加新功能、只做优化。本版安装包（安卓 versionCode 74、iOS build 74）是一次界面打磨：

- 修复：从编辑页返回书架后，左上角可能留下一枚无意义的返回箭头（书架本不该有返回）。页内顶栏改为订阅导航栈状态，箭头随栈同步。
- 主题：纸卡片纯白、投影更柔和立体；设置行与空分区行动行带彩色图标砖（赤陶／杏／靛蓝／墨绿），一个功能一个颜色锚点。
- 书架：只有一本时月册收成整宽书册行，不再孤零零靠左留白；相册／信／系列的空分区说明变成可点按的新建行；「最近」卡照片 16:10，区块间距收紧。
- 我的：顶部宝宝档案卡（印章首字 + 衬线名字 + 生日年龄，点按进资料页），tagline 降为居中脚注。
- 编辑页：照片／拍摄／说一段／AI／文件收成贴底图标工具栏（在保存按钮上方、随键盘）；「今天的小问题」一行小字；正文框去掉冗余标签；「从文件添加」进工具栏。
- 月册日期分区标题吸顶；年度册页头改为扉页式印章年份。
- 设计文档同步：DESIGN.md 记录以上全部变更。

## 1.0.0 — 传家册（构建号 73；含 Build 73–76）

主人 2026-09-21 拍板：四个模块做完作为 1.0.0 发版，此后不加新功能、只做优化。本版安装包（安卓 versionCode 73、iOS build 73）一次带上 Build 73–76：

- **说一段**（Build 73）：录完自动转成正文初稿；iPhone 本机识别，安卓或没有中文离线识别时逐段同意后经主人的服务转写，服务器不留声音。
- **出生的故事**（Build 74）：出生那天、怀孕的日子、名字的来历、我们怎么认识的，每题六问、一次答一问；当年的故事排在纸书第一章起。
- **访谈者**（Build 75）：AI 面板「追问我」、登录后由 AI 出「今天的小问题」、写信前的引导；生成／润色带落款、寄语带她说的话；同意书升版。
- **年度册的编者**（Build 76）：AI 建议目录（每月挑选、章首原话、书名），家人拍板后按目录装订；目录随家人同步、归档不带；备份提醒认得家人一起写。
- 服务端配套（须先部署）：转写端点、四个新 writingMode、八条提示词按 `docs/AI-PROMPTS.md` 逐字重写；出包流水线补 server typecheck 并在打 `v*` 标签时自动发 GitHub Release。

各版细目见下面四节。

## Build 76 — 年度册的编者

- 年度册导出纸卡新增「AI 建议目录」：总同意之外每次明确确认整年文字范围，不含照片和别的年份；建议书名、每月 1～3 条与逐字原话引语，先预览、可移除，家人采用才保存，可停止。手机端再次校验结果。
- `yearPicks` 按年随家人同步，同年冲突取更新时间较晚的；私有备份保留，开放归档不带。纸书按目录顺序装订，章首引用原话，装订前剔除已删记录与不再吻合的引语；未列月份保持全收，故事章、寄语、第一次与长图不变。
- 书架备份提醒认得最近 7 天成功的家人同步，说明同步仍不是应用之外的备份；提醒条件和沉默期不变。新增契约、同步、备份与装订回归测试，不新增依赖、不改构建号。

## Build 75 — 访谈者

- AI 面板新增「追问我」：只看当前事件的文字、落款、月龄、日期、故事主题与最近 10 条标题，问 1～3 个问题；点选后追加到正文，第一次只建议、确认才标记，不送照片或别的记录的正文。
- 「今天的小问题」在登录且已同意 AI 后每天最多请求一次，缓存仅留本机；失败或未登录静默使用本机清单，「换一个」不再请求。故事问题卡沿用固定六问。
- 写信正文下新增「不知道从哪开始？」：按落款、月龄、拆封日期与当前草稿给 2～3 个引导问题，点选追加，可停止。
- 生成与润色补送落款，年度寄语补送当年「她说的话」首段。同意书升至 v2，旧同意需再确认；各入口共用范围说明。新增上下文隐私、问题契约、每日请求与缓存校验回归测试；不新增依赖、不改构建号。

## Build 74 — 出生的故事

- 书架合集常驻「出生的故事」：出生那天、怀孕的日子、名字的来历、我们怎么认识的四个主题，每题六问，一次答一问；已写的还能再写一段，主题下可回读最近三段时光。
- 故事沿用普通记录、照片、说一段与落款，编辑时可前后翻问题，也可改回普通记录。出生那天默认用生日当天本地 10:00，其余主题用记述日期，发生时间留在正文里。
- 阅读、搜索、开放归档和离线网页带故事主题；当年故事按四主题顺序排在纸书寄语之前，月章不再重复，长图与整年统计不变。纯本机固定问题，不新增依赖、实体种类或格式版本，不改构建号。

## Build 73 — 说一段

- 编辑页「说一段」录完自动转成正文初稿，接在等待期间最新写下的内容后；分组时追加到第一件事。录音先保存，转写失败或停止都不丢附件、不改已有正文；写信仍只附录音。
- iPhone 优先使用中文本机识别；安卓或本机不可用时，登录家人账号并逐段同意后经主人的服务转写（小米 MiMo），可选「以后都同意」。服务端不保存声音，一段计一次写作额度，服务转写限 3 分钟／5 MiB。
- 转写中显示「正在转成文字…」与文字级「停止」；未登录、权限拒绝、空结果与失败就地说明。增加路线、上传、同意与取消回归测试，以及两平台编辑页入口断言。

## Build 72 — 家人一起写

> 定位见 `PRODUCT.md`：不是相册，是一家人写给她的传家册。这一版先给每段时光落款，再让几台手机一起写同一本册子。服务端「家庭对象空间」已于 2026-09-21 部署（SOURCE_SHA `473339b`），对 Build 71 的 App 向后兼容；本版 App 走新的按设备清单接口，要求服务端不早于 `473339b`。

- 落款：每段时光都有「谁写的」，用关系称呼（爸爸／妈妈／外婆…）而不是账号名。编辑页正文框下一行「—— 爸爸」／「谁写的？」，点开选称呼或自填（1–20 字）；第一次选定就成为这台手机的默认落款（「我的 → 我的落款」可改）。阅读页、纪念卡、纪念册、开放归档、年度统计与搜索（「谁写的」chips）都能看到落款；有默认落款且还有没落款的旧记录时，书架出一张「都写上「爸爸」」的卡，一键补上（只写落款，不改修改时间）。
- 家人一起写：一台服务就是一家人。登录家人账号后，在「我的 → 备份与恢复」末尾的「家人一起写」卡按服务状态给出唯一入口——家里还没人发布过：「开始一起写」（沿用或生成钥匙，直接进恢复码页）；家里已有手机发布且本机钥匙相同：「继续一起写」；否则「加入」（输入家人手机上抄下的 12 个词）。每台手机发布自己的全量清单，拉别人的做三方合并：文字、照片、相册、系列、人物、信与年度寄语都合在一起，草稿、选材与设置留在各自手机上；删除靠墓碑，别人删的不会被送回来。同步进度、结果与错误都在卡内一行文字上，不弹窗；「查看恢复码」「验证远端」照旧，「退出一起写」只撤下这台手机的清单，主人另有「删掉全家的远端」。
- 两台手机都改过：同一段时光两台都改，时间新的一版留下，另一版留底——书架出一张「有 N 段两台手机都改过」的卡（✕ 只当天不再提），点开「两台手机都改过」页逐条对照「留下的一版／这一版」的落款与时间，「用这一版」换回、「知道了」只收起留底。换回时会一并修好相册／选材封面与系列条目的引用。两台先后发布时输的一方自己的手机也留底：每段时光与每封信都带着前几版的版本世系（`ancestors`，内容哈希前 16 位、最多 8 枚），对方那版不是在本机这版之上改的就出卡；升级前的旧版本没有世系，不误报。
- 自动同步（默认开）：已加入时回到应用、以及保存一段时光后 30 秒自动同步一次；进行中家人卡显示「正在同步…」并禁用动作，备份页顶卡状态行说明；连不上服务这类暂时失败不记错误、下次回到应用再试，其他失败只在卡内与「我的」副题（「上次同步没成功，点开看看」）说明。本机备份／恢复／归档进行中不自动同步，反过来同步中这些操作也在状态行说明原因。「外观设置 → 家人一起写」可关掉自动同步，手动「现在同步」照常。iOS 拉通知中心这类短暂离开不中止同步，真正切到后台才中止。
- 扉页：「我的 → 资料」可填本名与名字的来历（都可选，本名 ≤ 20 字、来历 ≤ 60 字）。应用内扉页、纸书扉页、开放归档页头同款同序：名字 → 「本名 · 小名昵称」→ 衬线的来历句 → 生日。「我的」页 App 名后那半句改为「入淮清洛渐漫漫」。
- 不再有「从远端恢复」：换手机就是「加入」（空库合并等于全量拉取）。「远端备份」卡由「家人一起写」卡取代，testID 不变。
- 同步的代价：家人同步自己的清单单独放一份（`sync/manifest.xmbm`），不进「本机保留的备份」、不占三份保留位；内容没变（实体段与远端里自己的索引都没变）就不重传清单、不重发索引。
- 「我的」副题：「备份与恢复」按 冲突数 > 正在同步 > 上次同步没成功 > 上次同步时间 > 导出天数 的顺序带出状态。
- 服务端：家庭对象空间 `<root>/family/objects`，清单按设备存（`backup_manifests_v2`，`GET /backup/manifests`、`DELETE /backup/manifests/:deviceId`、管理端 `/admin/backup`），配额按家庭算，prune 的 keep 并上全部设备的清单；旧的按成员目录迁成 `legacy:` 行。全项目审查的服务端修复（回收与配额的数据丢失、对象覆盖、撤销语义、usage 性能）已在 main，**尚未部署**。
- 工程：`mobile/src/sync/` 新增 `merge.ts`（纯函数三方合并）、`family.ts`（一次同步／加入／退出）、`auto.ts`（自动同步排期）、`conflicts.ts`、`FamilyCard.tsx`、`Conflicts.tsx`、`status.ts`；`RemoteState` 升 v2（`seen`、`autoSync`、`lastPush`），新增 `base.json`（合并之基与已知版本）、`conflicts.json`（留底）；`local/hash.ts`（规范化 JSON 与内容哈希，`contentHashOf` 排除 `revision`／`updatedAt`／`ancestors`）；records／letters 加可选 `ancestors`，profile 加可选 `fullName`／`motto`；宪法第 6 条：`src/sync`／`src/ai` 不得 import 本机页面组件（只放行 `ui`、`context`）；删掉 Build 71 的单机入口 `runRemoteBackup`；端到端新增「两台手机一起写」（真服务端子进程，两份 SQLite 库）；双端冒烟走一次落款。mobile 测试 341 → 597。

### 同一版一并打包：书架重排

> 主人真机截图（2026-09-20，iPhone 深色、只有 1 段时光）：每个区块一条横向封面条，各只有一张封面靠在左边、同一张照片重复四次、区块之间大片空白，首页拉了两屏半。

- 书架只保留两种横向封面条：「最近」与每个年份名下的月册。年份成为书架本身——衬线年份标题 + 一行「N 段时光 · N 个第一次」+ 右侧文字级「翻开年度册」，不再单独摆年度册封面（它和月册封面是同一张照片）；不在最近 6 个月里的年份收成「往年」几行。
- 「最近」改成翻页条：整宽时光卡（照片 4:3 铺满卡顶 + 日期 + 衬线标题 + 正文两行；没照片就是纸面上的几行字）左右翻最近 10 段，右侧露出下一张的边、下面一排圆点。只有一段时光时首页顶上就是一张大照片，不再是一个 104 的小方块。
- 「随便翻翻」：「最近」标题右侧（不少于 3 段时光时出现）随机翻开一段时光，阅读页顶栏多一个「再翻一页」可以一直翻，不会连着翻到同一段，返回一次就回书架。
- 合集、专题册、时间胶囊、时光系列改成书册行：60×45 小封面（照片缩略图或纸面小签）+ 衬线书名 + 一行说明 + 右箭头，几本一组放在一张纸卡里；只有一本时也是整齐的一行。
- 间距：区块之间 24、区标题到内容 8、封面到书名 6、书名到说明 1；只有一段时光时首页约一屏。
- 工程：`SettingsRow` 支持 `leading`（自定义前导视图）与 `serifLabel`；`Photo` 支持 `radius`；`Record` 路由多一个可选 `shuffle`；纯函数 `shuffle.ts`（mobile 测试 338 → 341）。冒烟依赖的 testID 不变（`volume-YYYY-MM`、`volume-year-YYYY`、`album-*`、`letter-*`、`series-*`、`album-new`、`letter-new`、`series-new`）。
- 服务端：`sweepTemp(0)` 不再按 `mtime < now` 比较（mtime 有亚毫秒精度，同一毫秒内写下的临时文件会漏掉，CI 偶发红），0 宽限一律清空；无格式变化。
- 定位：新增 `PRODUCT.md`——不是相册，是一家人写给她的传家册：文字与声音是主角、照片是插图，每段时光该有落款，AI 从代笔改为访谈者与整理者（只出问题、转写与索引，不写正文），不做云相册／育儿工具／社交／AI 故事书，每个功能过三问。README、DESIGN 开头与 HANDOFF 路线跟着改：Build 72 改为「家人一起写」（落款先于同步），原 73「分享」不再单列。主人同日拍板：落款用关系称呼、转写先本机后可用小米 MiMo 一类、「按照片写标题正文」留着按「起个头」重写、72 与 73「说一段」都做；新增 `docs/AI-PROMPTS.md`（AI 提示词手册：原则、共用底座、八条提示词全文、客户端送什么、怎么验、转写的路）。App 里那半句话待主人选定。

## Build 71 — 复查修复

> Build 70 交付当天复查修出的一批，功能与格式与 Build 70 相同。服务端修复已于 2026-09-20 部署（对 Build 70 的 App 向后兼容）；本版 App 写远端清单时会带 `objects`，要求服务端不早于 `81d1ffe`。

- CI：push 触发的 `ci.yml` 也给 server 装依赖（端到端测试要拉起真实服务端），此前 main 连红三次；两个工作流的 quality 步骤现已一致。
- 服务端对象库：同 id 重传按「比原来多出的字节」算配额（此前 id 已存在就免检，换大可无限占盘）；`PUT /backup/manifest` 可带 `objects` 登记清单引用的对象，prune 永不删它们、远端已有清单时空 keep 一律 400（客户端给错、给漏、给旧 keep 都删不掉当前清单指向的东西）；删库先删索引再删对象；启动时清空临时目录。
- 远端备份界面与状态：`state.json` 改同步换名（此前备份刚成功，卡片重读状态时文件还没到位，会退回「开启远端备份」）；远端上传期间整个备份页上锁，删除保留备份也走统一流程；「正在整理照片」阶段按停止不再显示成红字错误；离开页面即中止上传／恢复；钥匙串读不出来时有一行说明而不是按钮全灰；恢复码页的停止对后半段的本机恢复也有效。
- 本机备份：备份／恢复完成后的收拾（清旧份、回收 blob）出错不再把已成功的操作报成失败；第一次把照片整理进 blob 库前先看空间，不够就一句人话拒绝；只有源文件读不满才说「备份素材不完整」，目标写不进去按原因报（不再把好备份说成坏的）；多卷恢复先一起核对每卷长度；停止精确到块；远端恢复先落一枚 `restoring-*.xmbm.part` 钉子，中断后已下载的照片不会被回收，续传真正成立；几处句柄与半成品的清理。
- 工程：mobile 测试 335 → 338；`verify-service.py` 备份段跟上新契约（清单登记对象、空 keep 应 400）。

## Build 70 — 传家 · 中

- 本机备份改成「照片只存一份」：应用内保留的三份备份不再是三份整包，而是三份很小的清单（`.xmbm`），照片与录音按内容哈希放进本机 blob 库（`anan-v1/blobs/ab/<sha256>`），三份保留备份只占一份照片的空间；写入先落 `.part`、长度与 sha256 双验再改名；回收只删「所有清单都不再引用」的字节，任一清单读不出就这一轮不删。备份页每份显示的是「清单 + 它引用的照片」的真实大小。
- 导出分卷：「导出完整备份」先写清单，再从 blob 库拼成 `.xmb` 交给系统分享面板；装得下时与 Build 68 的 `.xmb` 逐字节相同，超过 2 GB 自动分成几卷（`-vol1of3`），一卷一卷保存，进行中可「停止」。恢复时把几卷一起选中即可，乱序也认；缺卷、重复、混选、他份的卷都用人话说明。旧的 v1／v2 `.xmb` 照样能恢复；恢复最旧的那份保留备份时不再先被清掉（旧版会失败）。
- 加密远端备份（可选，在「我的 → 备份与恢复」最后一张卡）：登录家人账号后可以开启；开启时生成一把钥匙，展示为 12 个英文词的恢复码——恢复码就是钥匙，没有口令，抄在纸上就是整份远端备份的唯一钥匙。照片、录音与记录在手机上按 1 MiB 块用 XChaCha20-Poly1305 加密后再上传，对象 id 与 nonce 都由钥匙按内容派生，服务器只见密文、对象 id 与字节数，见不到照片哈希、文件名或任何内容；重装、换手机、断点续传都算得出同样的对象 id，所以只传远端缺的。「验证远端备份」核对索引、清单与每一份对象都在；「从远端恢复」输入 12 个词，缺的照片逐份下载解密进 blob 库，再走与本机备份完全相同的恢复路径，恢复后这台手机改用这份恢复码继续备份。「关闭远端备份」可以只关闭（远端留着，恢复码仍有效）或同时删除远端。
- 服务端（已于 2026-09-20 部署到 capsule.yep.li，对旧版 App 完全向后兼容）：新增 `/api/v1/backup/*` 对象库——对象以文件系统为准（`/data/backup/<成员>/objects/`），流式落盘边算密文哈希，单对象上限 8 MiB，每成员默认 20 GiB 配额（主人可在管理页调整）、磁盘剩余低于 5 GiB 一律拒收、每成员同时最多 2 个上传；`prune` 只删不在 keep 且创建超过一小时的对象；成员可删自己的远端备份，主人可删任一成员的；`manage.ts wipe-backup` 兜底。`verify-service.py` 顺带走一遍对象库，`probe-upload-limit.py` 探反代请求体上限（本次探过 16 MB 直达服务，反代无需改）。
- 宪法修订：`mobile/scripts/local_boundary.py`（可导入、带测试）——联网只允许在 `src/ai/client.ts` 与 `src/sync/transport.ts`；`src/local/**` 只有 App.tsx 与 Settings.tsx 可以 import `../sync/`；服务地址字面量只在 `brand.ts` 的 `SERVICE_URL`。AI 凭证读写抽到 `ai/session.ts` 供 AI 与远端备份共用。
- 工程：新依赖 `@noble/ciphers`、`@scure/bip39`（`@noble/hashes` 升到 2.4.0，只有一份）；主密钥只从 `expo-crypto` 取随机（Hermes 没有 `crypto.getRandomValues`）；传输层用 XMLHttpRequest（二进制体与 arraybuffer 响应），HttpClient 可注入；`tests/sync-e2e.test.ts` 拉起真实服务端子进程跑完整闭环（CI 的 quality 作业多装一次 server 依赖）；expo-file-system／expo-sqlite 的测试假件抽到 `tests/helpers/`。测试 mobile 299 → 335，server 20 → 30。双端冒烟：iOS 核对每个 blob 的文件名就是内容 sha256、保留备份都是 `.xmbm`；两端断言离线时远端卡只有「去登录」。Node 端 XChaCha20-Poly1305 封装 64 MiB 约 220 MB/s；真机 MB/s 待首个真机版实测后记入 HANDOFF。

> 注意：本版起应用内保留的备份是 `.xmbm` 清单 + blob 库，导出的 `.xmb` 超过 2 GB 会分卷——这两种只有 Build 70 及以后能读。请不要在升级后再降级回旧版本；给旧版看的整包只在单卷（≤ 2 GB）时兼容。

## Build 69 — 界面整顿

- 页头：原生页头下线，所有页面由 `Page` 在安全区顶部自绘一行「‹ 返回 + 标题」；表单与设置类页面（编辑、写信、我的、备份与恢复、外观、AI 设置、搜索、选材）标题在顶栏，内容类页面（阅读、相册、年度册、系列、读信）顶栏只有返回、标题随内容；装订预览不再压住状态栏时钟；iOS 侧滑返回与 Android 硬件返回照旧。
- 书架：头部是衬线名字 + 年龄行，右上角搜索与「我的」名字圆章（读屏标签「我的」）；提醒卡同屏只留一张（里程碑 > 装订 > 备份 > 节奏），都能关（里程碑与节奏当天、备份 7 天、装订本季，关闭时间记在库里）；各区改成横向小封面条（封面固定 4:3 裁切），「最近」在最上，区标题右侧是文字级的新建入口，空区一行说明、不再摆「+」虚位册；月度册只放最近 6 个月；那年今日在「最近」之后；空库首屏只有一张「把今天的小事留下来」纸卡；扉页没填名字时不再拿应用名充数。
- 阅读页：先照片（多张横向分页连翻）与正文，动作收进底栏（「编辑」主按钮、「加入相册」、「纪念卡」）；「加入相册」就地展开现有相册与「新建相册」，不再绕进选材页；录音显示为「听录音」而不是文件名；「第一次」「她说的话」是带勾的开关；「删除记录」在页尾。
- 相册、年度册、系列页：动作一行分主次，寄语卡移到页尾；年度册「导出成长册」就地展开「长图」「纪念册 PDF」，不再用系统弹窗当菜单，「这一年没有照片」改为一行说明；空态都给出下一步（「记一刻」或「回书架」）。
- 编辑器：正文框永远在最上，加照片后不再消失，下面一行「草稿会自动保留」；按拍摄日期分成几件事改为开关（默认关；从其他应用分享进来且照片跨多天时默认开）；标题、地点、人物收进「更多」；「放弃这份草稿」改为页尾危险文字级；保存进行中按返回会等保存完再走，不再弹红字；AI 面板底板改实色纸面、模型名移到脚注。
- 空实体清理：点开就建的草稿、信、时光系列，什么都没填就退出时静默删除，不弹确认，也不会三天后被催。
- 「我的」：改为分组设置行（宝宝资料、备份与恢复、本机存储、AI 设置、外观设置），副题直接带出生日、上次导出、占用、登录状态与当前主题；备份页三张纸卡、一页一个主按钮，本机保留的备份按「9月19日 15:44 · 0.1 MB」显示、不露文件名，开放归档按钮降为次级；外观页主题是一行勾选式按钮；本机存储的「本机健康」收进「查看本机健康」；AI 设置首段不再以模型名开头，断网只说「现在连不上服务，请稍后再试。」。
- 文案与图标：记录一律数成「N 段时光」，附件按种类「N 张照片／N 段录音」，混合时「N 个附件」；「素材／校验／.xmb」退出正文只留脚注；写给她的用「你」、写关于她的用「她」；`sparkle` 只表示 AI，新增 `quote`（她说的话）与 `pin`（地点），`heart` 只留纪念卡与寄语卡。
- 工程：`Button` 四级（主／次／文字／危险）与选中勾、`GlassDepth`（纸卡与底栏内不再套玻璃）、`SectionHeader`／`SettingsGroup`／`SettingsRow`、`useTopBarOffset`；库根新增 `nudgeClosedAt`；纯函数 `pickNudge`、`appendToAlbum`／`newAlbumFrom`、`captureDayCount`、`isEmptyDraft`／`isEmptyLetter`／`isEmptySeries`、`backupStampLabel`、`birthdayLabel`；删除零引用的图标与插画资源。测试 mobile 275 → 299，server 20 不变。双端冒烟：「我的」入口标签、iOS 返回改点 page-back、补 home-recent 与 record-bottom-bar 截图。数据格式与服务端接口都没变，没有降级限制。

## Build 68 — 传家 · 上

- 时间胶囊信：书架新添「时间胶囊」区，可以给多年后的她写一封信——标题、正文、落款，可以附一段录音；封存以后就不能再改，默认到 18 岁生日那天才能拆（没填生日就从今天起数 18 年，日期也可以自己定），拆封那天书架上的信会写着「可以拆了」，等不及也可以提前拆封（要确认一次）。信随备份一起走，「清理未使用素材」不会误删信里的录音。
- 开放归档：「我的 → 备份与恢复」新增「开放归档」——把资料导出成一个普通的文件夹压缩包（.zip）：每条记录一个文件夹，正文是带日期／地点／人物的 Markdown，照片、视频、录音按原样复制并起人能读的名字；相册、时光系列、寄语、信、人物各一份清单；另附一份 library.json 和一个离线网页 index.html，在电脑上解压后双击就能按年月翻看、播放录音与视频，不需要这个 App，也不连网。可以只导某一年；未拆封的信默认不带（归档里的信是明文）。导出前先查本机剩余空间，过程中可以停止，素材缺一份就整份不写。
- 她说的话：阅读页可以把一条记录点亮为「她说的话」，那句原话会收进书架的「她说的话」册（衬线大字引正文首段，下面是日期与当时的月龄）；搜索多一个「她说的话」筛选；归档的 front matter 与离线网页里都有这一栏。
- 装订提醒：每年一二月，如果去年有记录而纪念册 PDF 还没装订过，书架会出现「去年的纪念册可以装订了」纸卡，附「去年度册」入口；装订过一次就不再提。
- 工程债：仓库卫生（`.commandcode/` 出库、去掉四个零引用依赖、旧计划归档到 `docs/plans/`、`.nvmrc`）；服务端密码哈希带上参数（`scrypt2:N:r:p:盐:密钥`，N 提到 32768，旧哈希登录成功后自动升级；兜底改密命令按登录名或成员名都能找到人）；一次改动没碰过的集合保持引用不变，书架、年度册与搜索按集合记忆派生数据——改一条草稿不再让一万条记录重新排序。
- 工程：新增零依赖的流式 ZIP 写入器（只存不压、UTF-8 文件名、数据描述符边读边算 CRC32，超过 4GiB 自动切 ZIP64），测试里由 Python zipfile 当独立裁判；归档版面与离线网页都是纯函数／纯字符串，各自有单元测试；录音钩子泛化后写信页与编辑页共用。测试 mobile 223 → 275、server 18 → 20；双端冒烟新增「写信 → 封存 → 重启 → 提前拆封」与「导出开放归档 → 分享面板出现 zip」两段，iOS 驱动脚本用 zipfile 校验缓存里的归档。

> 注意：写过信的库，其备份只有本版及以后能恢复（旧版不认识 `letters`）。请不要在导出新备份后再降级回旧版本。

## Build 67 — AI 改为账号登录

- 家人各一个账号：空服务第一次在「我的 → AI 设置」创建主人账号（仅此一次），家人账号由主人在管理页创建（用户名＋初始密码）；换手机直接登录，一个账号可挂多台设备，丢手机重新登录即可——不再是「重新邀请」，也没有公网陌生人直接进来的口子。邀请码与中间过渡的开放加入（未部署过）一并成为历史。
- 2026-09-19 已随本版上线服务端（capsule.yep.li，同时完成 xiaomei-ai→anan-ai 的目录、compose 与备份 unit 改名迁移），并按主人指示清库从零：装上本版后第一个进 AI 设置的就是「创建主人账号」。
- 密码体系零新增依赖：node:crypto 内置 scrypt（N=16384、随机盐、timingSafeEqual）；登录失败统一「用户名或密码不对」，用户名不存在时也跑同代价派生防时序探测；`/setup` 与 `/login` 限速（登录每 IP 每分钟 10 次）；自己可改密码（要过当前密码），主人可给任何成员设置或重置登录；全部锁死时 `manage.ts password <成员名> <新密码>` 在服务器兜底。
- 服务端接口：`GET /status`（是否已初始化）、`POST /setup`、`POST /login`、`PUT /password`、`POST /admin/members`、`PUT /admin/members/:id/login`；旧库启动时自动补 `username`/`password_hash` 列（部分唯一索引，过渡期允许多个未设登录的成员），已有设备凭证、额度与用量不受影响；所有对外返回的成员对象一律剔除密码哈希。
- 手机端 AI 设置页：未登录时按服务状态显示「创建主人账号」或「登录」表单；登录后可改密码、退出登录；主人管理页新增创建家人账号与成员「保存登录」（旧成员显示「未设登录」标记）。
- 测试与校验：服务端测试的鉴权部分重写（一次性初始化、登录校验、凭证撤销失效、建号与重置、管理端角色隔离、响应不含密码哈希）；`verify-service.py` 覆盖新库初始化与复跑两条路径，顺带验证兜底改密命令。
- 同日质量走查修复：AI 设置页在凭证被服务端作废时自动清除并回到登录表单（此前会困在按钮永久禁用的死胡同）；传输层不再把网关返回的 HTML 错误页误报成「网络中断」；改密不再 trim 当前密码，与登录一致。
- 服务端记账与安全闭环：已完成的 AI 请求不会再被迟到的错误覆写成 failed、tokens 清零；改密接口补上限速；自己改密只保留当前设备、主人重置登录会把该成员全部设备下线。
- 工程重构与清理：`ai/Editor` 的 generate() 抽出纯函数编排层 `plan.ts`；人物 chips、月份标签、月册搜索谓词、书架印章与列宽计算收归单源（新增 `PersonChips`、`useVolumeWidth`、`DateStrip`、`recordMatches`）；删除三个无人引用的死导出与上一代 Next.js 留在仓库根目录的遗留目录。测试 mobile 195→223、server 13→18（新增 ai/client、passwords、节流、改密撤销、generate 编排等）。

## Build 66 — 界面走查修复

- 按 DESIGN.md 对全部界面做了一次走查，修掉与规范明文冲突的几处：搜索页补上缺失的 20 页面边距（搜索框、筛选与结果不再贴屏幕边缘）；年度重放末页不再出现选乐入口（规范本就如此要求，代码里自相矛盾）；重放剧场是固定暖黑语义，末页按钮与印章不再随深浅色变色，剧场强调色固定在 overlay 色板单源。
- 阅读页同一记录的多张照片可以「上一张／下一张」页内连翻（与查看素材页同款交互），不再全部纵向铺开；查看原图直达当前这张。
- 页头统一：各页面的衬线大标题只出现一次（导航页头标题留空，与月册／年度册既有做法一致），保留的页头标题（搜索、查看素材）也走衬线；Android 页头从卡片白改成纸色，与 iOS 连成一片。
- 一致性小修：相册选材的月份筛选显示「2026年9月」而不是原始 `2026-09`；「AI 设置」改用星火图标，不再与「外观设置」共用齿轮；外观页开关换成赤陶色系；选材浮层补上 `accessibilityViewIsModal`（读屏不再逃出浮层）；装订预览的「开始装订」改为主按钮样式；年度册空年份只留一处空态；阅读页记录标题回到 18/600 的记录标题规格。
- 工程：纪念卡／成长册／纪念册的导出 SVG 字体从手写的 `"Georgia, serif"` 字面量收回 `serif` 令牌单源，Android 导出件与屏幕字体不再分叉；足迹页脚注去掉越界的衬线；阅读页删除钮回到辅助字号刻度；相册选材空态去掉叠加成 40 的双重边距。195 个测试全部通过。

## Build 65 — 纪念册交付验证修复

- 双端纪念册冒烟直接断言系统分享面板里的文件名，避免小册子生成太快、错过进度按钮而误报失败；iOS 还读取成品验证 PDF 完整性与多页结构。
- iOS 启动命令限时 300 秒，只有命令超时才重试一次并保留日志；应用崩溃、启动命令明确失败、界面断言失败仍直接报错。
- iOS 回归未执行时不再上传不存在的证据目录，保留真正的首个失败原因。
- 本轮占用 Build 65 构建号；原计划的相册成册与自由编排尚未交付。

## Build 64 — 桉桉 · 上

- 孩子改叫桉桉：产品名换成「桉桉成长记」，扉页、纪念卡与成长册落款、iOS 分享扩展显示名、服务端落地页一并跟上。名字本身仍然在「我的 → 宝宝资料」里随时能改，这一版改的是产品自己的名字。
- 内部标识也一起改了姓：本机文档目录、本机库文件、原生共享收件箱、AI 设备凭证键、备份文件名前缀全部从 `xiaomei-` 换成 `anan-`。**旧名的资料会在第一次启动时自动搬过去**，一个字节都不会丢：目标已经存在的文件绝不覆盖，搬不动就原样留在旧名上继续用，失败摘要记进「本机健康」。改名前在途的分享、改名前恢复出来的库、改名前导出的备份，全都照常认。
- 年度册可以真送印了：除了原来的长图，新增一本 20×20cm 方形开本的纪念册 PDF——真分页、每页独立排版，封面（满版照，可手选）、扉页、按月分章、每条记录先文字后照片、「第一次」清单、寄语与落款，内容页排页码，四周留 3mm 出血，300 DPI。以前那条「把长卷按 A4 比例盲切」的路已经拿掉：它会把照片和正文从中间腰斩，分辨率还随设备屏幕在 90–270 DPI 之间漂。
- 顺带补齐几处一直在静默丢内容的地方：「第一次」不再只进 8 条，寄语不再只进 14 行，整年不再超过 6000 像素就截断，一条记录的照片一张不落地进册子。照片按它在版面上的位置取图，绝不放大；装订完会说清楚有几张原图撑不满版位、印出来会偏软。
- 装订之前先翻一遍：点「纪念册 PDF」不再直接开工，而是进逐页预览——整页按屏幕大小渲染，一页页翻着看，页数与开本写在最上面，满意了再「开始装订」。装订过程逐页显示「正在装订 N/M 页」，随时可以停。
- 工程：新增纯函数排版引擎 `book.ts`（成册几何、分页、版位）与 13 个针对它的测试，断言「没有元素跨页、没有元素越出安全框、一条清单都不丢」；改名迁移与年度册装配也各有一组纯函数测试，整套 163 → 195 个；导出改成逐页取图、逐页落盘、边写边生成 PDF，任何时刻内存里只有一页；核过两个平台 `toDataURL` 的原生实现——安卓按给的像素另开位图，iOS 只认视图自身的点数，两边分别换算，成品分辨率不再随设备漂；模拟器启动等待从 5 分钟放宽到 10 分钟，且超时后不再抹掉一台正在启动的设备（CI 上连着两次 300 秒超时就是这么来的）。

## Build 63 — 十年之库·上

- 编辑不再越记越慢：以前每改一个字都要把整个资料库重写一遍落盘，记录攒多了就会拖慢保存。现在只写真正动过的那一条。本机实测一万条记录、一万段素材的库，改一个字从 90 毫秒、写 6.2MB 变成 10 毫秒、写 489 字节。旧库在第一次打开时自动换成新结构，内容一条不差；换库失败会原样保留旧库，下次启动重来。
- 备份换了格式：清单不再是一整块 JSON，因此不会再随着写得多而撞上「备份清单过大」；同一张照片在备份里只存一份字节，备份文件更小；导出时素材只读一遍（原来要读三遍）。**Build 62 及更早导出的旧备份仍然可以恢复**，不需要做任何事。
- 恢复不再卡住界面：解包、校验与重建缩略图都挪出了写队列，一路显示进度（重建缩略图还带 n/N），最后只用一次很短的写入整体切换。中途任何一步失败，当前资料一个字节都没动过。
- 成长册多一种导出：除了长图，还可以导出按 A4 分页的 PDF，适合直接打印成册。本机生成，不走网络，不新增依赖。
- 工程：新增一万条记录规模的基准测试，把「一次改动要写多少字节」钉成可回归的断言；全库校验拆成根与逐实体两层，一次改动只校验动过的部分，遇到删除才整库重来（有 13 例等价性测试守着这条取舍）。

> 注意：这一版导出的备份用新格式，Build 62 读不了。请不要在导出新备份后再降级回旧版本。

## Build 62 — 成长册

- 年度成长册：年度册内页新增「导出成长册」，把这一年装订成一张 750 宽的竖向长图——印章年份封面、统计一行、「爸爸妈妈的话」寄语、十二月四列网格（每格月份标签与当月封面照）、最多 8 条「第一次」清单与落款。排版是纯函数 `yearbook.ts`，`YearBookCard` 离屏渲染后导出，与纪念卡、系列对比卡同一套纸面语汇；本机生成不走网络，无记录的年份禁用入口，超长内容从版面上限截断。
- 年度重放配乐：重放默认仍然无声，可从本机已有录音里挑一段做循环低音量配乐（`settings.replayAudioId`，受 `referencedMedia` 保护不会被「清理未使用素材」误删，指向非音频时自动剥掉）。选乐浮层打开时暂停自动前进，末页不再显示选乐入口。不引入任何联网或曲库。
- 整理人物：编辑器人物 chips 下方新增「整理人物」，按名字排列全部人物并显示各自被多少条记录与草稿用到，可改名、合并进另一个人、删除。合并与删除先确认，且只动标记不动记录——标记会从记录、草稿正文与「按事情分组」的每件事上一并剥离。`validateLibrary` 现在也校验每件事的人物引用，`normalizeLibrary` 在开库与解码备份时静默剥掉悬空标记，绝不会因为一个坏引用把人锁在自己的资料外面。
- iOS 26 液态玻璃：在 iOS 26+ 且未开启系统「降低透明度」时，`Glass`／`Card`／`BottomBar` 基元渲染为系统液态玻璃（强调处以赤陶 tint 淡染），悬浮钮与底部条同款；Android 与降级路径保持原来的实色纸卡片，两种质感共用同一套间距、字号与圆角。页头两个平台都是实色纸面（取纸色与页面底色连成一片）——导航栈的页头外观只有不透明与全透明两条分支，拿不到系统材质，因此不走玻璃。
- 手感与无障碍：拍摄、保存、里程碑与 AI 采纳加触感反馈；重放浮层重建在共享 overlay 色板上并补了字幕遮罩与读屏结构；书架与月册尊重系统「减少动画」；大字阈值、标题字号与间距在各页统一；Android 原图支持双指缩放、拖动与双击还原。
- 可用性修复：每日小问题在「按事情分组」时不再出现；相机／相册权限被拒时直接给「去系统设置开启」；相册封面与时光系列补月份改为弹出的虚拟化照片网格；编辑器键盘偏移改用真实页头高度；照片加载改为乐观渲染并逐级回退（缩略图→原图→占位），不再在渲染期查盘。
- 修复：Android 看原图时任意一次捏合收回或缩放后双击必崩（手势回调调用了非 worklet 函数）；年度重放底部字幕条吞掉「点击前进」，底部一整条成死区；月册页忽略系统「减少动画」。
- 工程：合并两份逐字重复的「记一刻」悬浮钮与两个照片选择器，后者顺带修好了嵌在滚动页里而实际失效的虚拟化；主题色值收回 `ui.tsx`（页面不再自带 hex），去掉一处手写哈希指纹与 `exhaustive-deps` 豁免；补齐人物管理测试（132→138）；`@react-navigation/elements` 由幽灵依赖改为显式声明；跟上 Expo SDK 57 的补丁版本（含复核 expo-image-picker 相机补丁在新版仍然适用）。修好 iOS 冒烟的模拟器重试——两次尝试之间的清理会抛异常并把重试本身干掉，模拟器开机故障因此直接判死。

## Build 61 — 记忆的秩序

- 人物标签：编辑器「补充信息」区可多选人物 chips 并就地新建（同名复用）；阅读页日期行下以辅助色小 chips 显示人物；全局搜索、选材页与年度册内页都支持按人物筛选；不做人脸识别，标签全由父母手选。人物与记录的 personIds 随 `.xmb` 备份保存，旧库旧备份无此字段可直接打开。
- 本机健康页：「我的 → 本机存储」新增「本机健康」区块——启动次数与最近一次启动耗时、写库次数与平均/最长耗时、写盘失败次数与最近一次错误摘要、最近备份时间。`health.json` 以 part→move 原子写入（平时 10 秒防抖合并、写盘失败即时落盘、退后台冲刷），坏文件自动归零，采集失败绝不阻塞记录主流程；全部数字只留本机，不上报。
- 轻量足迹：书架新增「足迹」入口，把照片拍摄位置按 250 米聚类（中心取组内第一张坐标），按照片数排序列出常去地点（封面照、张数、首末拍摄日期、中心坐标），点开即见该地点的照片所在记录。聚类抽成共享纯函数 `places.clusterPlaces`，AI 地点分组复用同一实现；无带位置照片时不显示入口。真地图（react-native-maps）按计划为可整体裁剪的独立提交，待轻量版验收后再上。

## Build 60 — 时光的礼物

- 时光系列（同款时光对比）：书架新增「时光系列」区，为同一姿势或场景每月收一张照片（月份取照片拍摄时间，缺省记录日期，同月后选替换、先确认）；内页按月排成成长时间线，首月至当月的缺口月份显示「还缺这一张」虚位卡并可把该月照片收进来；满两张可一键导出横排对比卡（最多 4 张、超限均匀抽样保留首尾，纸面卡片样式与纪念卡一致）；记录删除或照片移出时系列条目自动退场，随 `.xmb` 备份保存，旧库旧备份无此字段可直接打开。
- 每日一问：新草稿正文为空时，编辑器在输入框下显示一条「今天的小问题」——内置 120+ 条中文小问题，按月龄段（0-6 月／6-12 月／1-2 岁／2-4 岁／4 岁+）分组，同一天稳定不换（「换一个」当次换种子）；「不问了」当次会话不再出现，无设置项、不落库。
- 年度重放：年度册新增「重放这一年」，全屏幻灯回看每月封面与全部「第一次」的封面（按日期升序、上限 15 张），照片整屏淡入淡出、自动 4 秒或点击前进、底部进度点，末页收年度统计与「爸爸妈妈的话」寄语，可再放一次；尊重系统减少动画设置（开启时切换为无动画）；无照片年份按钮禁用；v1 不配乐。
- 记录节奏提示：距上次记录满 3 天时书架出现「有 N 天没记啦」温和纸卡（可关、当天不再出现，仅会话态）；有未保存草稿且草稿 3 天没动过时改为提醒「接着写」，新鲜草稿交给既有草稿入口，不重复提醒；无推送无弹窗。

## Build 59 — 数据安全与长期

- 应用锁（可选）：在「外观设置 → 隐私」开启后，冷启动与回到前台需要指纹、面容或锁屏密码（expo-local-authentication，本轮唯一新依赖）；退到后台即重新上锁；设备没有任何锁屏验证时不锁死用户。开关仅在系统已录入生物识别时可用。
- 全局搜索：书架顶部搜索入口，跨全部记录按标题、正文、地点不区分大小写检索；筛选：年份、只看「第一次」、有声像、纯文字；结果按日期倒序，点击直达记录。
- 年度回顾页：年度册内一键查看这一年的印章年份、统计一行、月度封面横排（可点进月册）、「第一次」清单与年度寄语；Build 60 会在此之上加重放幻灯。
- 签名策略维持 runner 模板 debug 证书 + 指纹钉住（已验证跨 runner 稳定）；私有 release keystore 待持有者决定后随时可换。

## Build 58 — 性能与媒体重构

- 持久缩略图：照片与视频入库时生成 512px 缩略图（expo-video-thumbnails 为本轮唯一新依赖），书架封面、网格、选材等列表场景渲染缩略图，阅读与缩放仍用原图；月册网格不再解码整批原片。
- 真实宽高比：记录像素尺寸并用于版面，替换三处写死的 4:3／3:4；旧素材与无尺寸素材退回默认比例。备份不含缩略图字节，恢复时以全新随机名重建，失败清理永不波及当前库文件。
- 输入防抖：编辑器正文与分组标题改为界面即时、落盘延后合并（400ms，失焦/退后台/保存前冲刷），相册起名改为失焦落盘，选材列表滚动位置去抖持久化——不再每个击键/每次滚动整库写盘。
- 批量导入：一次多选只做一次素材入库与草稿落盘。
- 增量校验：素材在导入时完整校验一次，保存时只查本会话新增；退后台时闲时巡检少量素材，跨会话逐步覆盖全库。
- 备份导出改用只读快照，不再占用写队列、不再虚增 revision。
- 同一条记录的多张照片可用「上一张/下一张」连续翻看。
- 重构：编辑器的草稿持久化与录音逻辑拆为 `editorHooks`（useDraftPersist/useRecorder）；清理 Glass 从未生效的 `intensity` 参数。
- 流水线：iOS 启动冒烟对系统横幅遮挡做重试；Android 冒烟对慢冷启动与 dump 抖动做重试加宽。

## Build 57 — 纪念的礼物感

- 书架头部年龄行：「2 岁 3 个月 · 来到世界第 825 天」（填了生日才显示）；出生当天、第 100 天与每周岁当天出现印章式里程碑卡，点击直接记下这一天。
- 「那年今日」升级：全部往年同日的记录都排进横滑卡片条，每一段都能翻到，不再只取第一条。
- 年度册封面印章化：无照片的年份册以双线印章圆环加年份数字作纸封面；扉页印章画法抽成共享 `Stamp` 组件。
- 相册扉页寄语：每本相册可写一段最多 2000 字的话，与年度寄语共用「爸爸妈妈的话」纸卡；随本机备份保存。
- 纪念卡导出：阅读页一键把这条记录生成一张纸底衬线卡片图（照片 + 标题 + 正文 + 日期 + 装饰线），可保存、分享或打印；纯本机渲染，不需要网络，也不新增权限。
- AI 年度寄语起草：编辑年度寄语时可让 DeepSeek 依这一年的记录标题与「第一次」清单写一版草稿（纯文字、不含照片与精确位置，计一次写作额度），核对修改后才保存。
- 拍摄地点一键换地名：记录里有照片坐标且地点为空或仍是裸坐标时，可按需逆地理编码换成地名（Android 需允许定位，iOS 无需授权），确认后才写入。
- 编辑器「移除」素材增加确认弹窗，误触不再丢素材。
- 修复：Android 分享接收的 GPS 读取改用 EXIF 度分秒字符串解析，解决打包机的 Kotlin 编译失败；新原生代码先在本机对平台 SDK 验证编译。

## Build 56 — 数据底线与确定性缺陷修复

- 分享接收加固：一条坏掉的分享素材不再毒死整批——坏条目跳过并计数、批次之间互不影响；写库失败时收回已复制的文件，不再静默膨胀；无可保存内容的批次直接确认，不再每次启动重放。
- 分享草稿补齐拍摄信息：从微信等应用分享进来的照片自动读取 EXIF 拍摄时间与 GPS（Android 声明 `ACCESS_MEDIA_LOCATION` 保留相册照片定位），草稿自动落日期地点并开启按天分组建议，与编辑器导入一致。
- 按天分组两处修正：先写文字再导入多天照片时，标题正文只留在第一组；无拍摄时间的视频／录音跟随相邻照片归入同一天，同一场合不再被拆散。
- 编辑器修复：iOS 分组日期选择器不再一闪即关（滚轮加「日期选好了」确认）；录音入库或放弃后清理原始文件；保存忙碌时返回手势给出提示而非静默吞掉。
- AI 额度公平：上游失败或超时不再消耗当日额度；请求已结束时隐藏「重试原请求」，只留「重新生成」并说明只有重新生成才计入额度。
- 备份守护：书架在有记录且从未导出或超 30 天未导出时温和提醒（不弹窗）；备份页显示上次导出时间；本机保留最近 3 份可读文件名的备份、超出自动清理并支持删除；启动恢复推荐位只推荐完整校验通过的备份。
- 测试与流水线：Android 冒烟新增备份导出／删记录／恢复闭环，iOS 原生回归覆盖年度册寄语，新增 fixture 与模型形状对齐测试，APK 签名证书指纹钉进打包验证。
- 细节修正：异常兜底页深色模式可读；浅色辅助文字对比度提升至 4.5:1；月册／年度册／我的页去掉多余的顶部留白；书架、第一次、月册空态补 Ornament 收尾；分享扩展显示名改为「小美成长记」；阅读页正文可长按选择摘抄。

## Build 55 — 年度册与年度寄语

- 书架新增「年度册」区：每一年自动汇成一册，按年倒序，封面取当年第一张图，标注时光数与「第一次」数。
- 年度册内页：这一年的统计（时光、照片、影音、第一次、字数）、「这一年的第一次」列表、当年月册小册，以及「爸爸妈妈的话」年度寄语（最多 2000 字，随本机备份保存）。
- 旧版资料库与旧格式备份自动补齐年度册字段，打开与恢复不受影响。

## Build 54 — 全面精致化

- 衬线标题加克制字距，卡片阴影更轻更柔，纸张层叠感更细腻。
- 无照片的纸封面左侧带赤陶书脊细条；扉页印章改双线圆环，扉页收尾加细线菱点装饰。
- 阅读页日期配强调色小竖条，媒体间距统一；「删除记录」弱化为辅助色文字按钮；记录张数与「第一次」角标改为纸面描边样式，深色下不刺眼。
- 页面转场改为淡入淡出；进入与按压动画尊重系统减少动画设置，开启时全部取消。

## Build 53 — 书架动画

- 书架活了起来：月度册与专题册进场时按顺序交错淡入上移，「那年今日」卡片与「记一刻」悬浮按钮带进场动画。
- 书册与「记一刻」按下时改为弹簧缩放、松手回弹，取代单纯的透明度变化。
- 动画风格克制，暖纸手账调性不变；大字号与单栏布局下表现一致。

## Build 52 — 纪念品书架首页

- 首页改为纪念品书架：顶部扉页展示册名与统计，月度册与专题册以书脊式 Volume 排列。
- 「第一次」合集与「那年今日」卡片直接陈列在书架上，翻阅即达。
- 移除旧底部 tab，相册等入口整合进书架与各册内页。
- 「记一刻」悬浮按钮保留在书架右下角，随时开始写记录。

## Build 51 — 暖纸手账主题

- 全应用改用暖纸手账视觉：暖纸底色、手账质感卡片与按钮，取代液态玻璃风格。
- 记录、编辑与设置各页面统一新主题；浅色、深色与大字号规则不变。

## Build 50 — 修复分组与润色上限

- 修复「按事情分组」：此前分组请求只带空照片列表，一次请求都不发出并直接报「AI 分组不完整」；现在按整份草稿的照片分组，至少两张即可正常分批整理。
- 修复确认分组丢照片：关闭「按拍摄日期建议分组」后确认 AI 分组，此前只有第一组留在草稿里，现在每件事都写回草稿。
- 润色上限改由服务端按正文计算，标题长度不再误报；标题与正文合计过长时先在本机说明，不再落到笼统的「输入内容无效」。
- AI 图标的小圆点只在结果确实待查看时出现：收起面板后才返回的结果会亮起，打开面板即视为已读。
- 点「AI 生成」「润色我的文字」会切到写记录任务，生成完成后直接展示结果，不再提示「还有一份建议待确认」。
- 「重试原请求」「重新生成」只在本地校验通过后出现；生成时正文超出单次上限会明确说明只发送前 N 字，不再静默截断。
- 底部面板标记为读屏模态；本机备份与分享测试改用 POSIX 路径写法，Windows 下不再误报。
- 没有设备凭证时先收起 AI 面板再进入「AI 设置」，不再把面板留在设置页上层；iOS 原生回归测试同步改用新的 AI 图标与底部面板标识。

## Build 49 — AI 编辑底部面板与润色

- 编辑工具栏改为单个 AI 图标（图标 22、触控至少 44），打开共享主题的底部面板，不再常驻大面板；有结果待查看时图标显示小圆点，收起面板保留任务与结果。
- 「写记录」提供「AI 生成」与「润色我的文字」：润色只发送当前事情的标题和正文、不发送照片；正文一次最多 2000 字，超限明确提示且不截断；结果与原文对照，可全部采用或只采用标题、正文，采用前不覆盖原文；纯文字草稿也能润色，采用后直接写回正文并随保存与重启保留。
- 「整理照片」提供「按事情分组」，用于至少两张照片的新建草稿；分组预览可调整照片归属，摘要不写入正文、不自动触发写作。
- 本地 AI 任务与建议保存写作模式；重试按操作、模式、所属事情和输入区分，生成与润色不再串用结果，旧数据缺少模式时视为生成；输入变化后旧建议过期，不覆盖新内容。
- 首页「记一刻」改为小型悬浮按钮；多图记录以张数角标展示，与「第一次」合并显示。
- 服务端对润色正文超限返回明确的 POLISH_TOO_LONG 错误；模型继续固定 DeepSeek Flash High，提示词集中在服务端维护。

## 0.1.0 — 液态玻璃（构建 44）

- 首个 0.1.x 发行版；历史预发行标签与发布已全部清除。
- 全面改用液态玻璃视觉：静态氛围光斑背景上的半透明模糊卡片、输入框、按钮与底部导航；iOS 26 使用系统玻璃，其余平台用模糊视图并回退半透明。
- 记录卡片、「记一刻」浮动按钮、编辑保存栏与选材底栏均为玻璃材质；浅色、深色与大字号规则不变。

## Build 43 — 本机成长记录

- 独立本机记录、草稿、素材、相册和选择会话。
- Airbnb 温暖照片风，统一苔绿主题、直接选材和全屏编辑。
- 完整本机备份、恢复前备份、文件校验与失败回滚。
- 移除 Web 服务端、账号、上传和同步流程。
- 使用全新本机数据库；旧测试目录保持原样，不自动迁移或删除。

旧版本历史保留在 Git 提交记录中。
