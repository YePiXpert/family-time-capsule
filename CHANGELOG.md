# Changes

## Build 58 — 性能与媒体重构

- 持久缩略图：照片与视频入库时生成 512px 缩略图（expo-video-thumbnails 为本轮唯一新依赖），书架封面、网格、选材等列表场景渲染缩略图，阅读与缩放仍用原图；月册网格不再解码整批原片。
- 真实宽高比：记录像素尺寸并用于版面，替换三处写死的 4:3／3:4；旧素材与无尺寸素材退回默认比例。备份不含缩略图字节，恢复时以全新随机名重建，失败清理永不波及当前库文件。
- 输入防抖：编辑器正文与分组标题改为界面即时、落盘延后合并（400ms，失焦/退后台/保存前冲刷），相册起名改为失焦落盘，选材列表滚动位置去抖持久化——不再每个击键/每次滚动整库写盘。
- 批量导入：一次多选只做一次素材入库与草稿落盘。
- 增量校验：素材在导入时完整校验一次，保存时只查本会话新增；退后台时闲时巡检少量素材，跨会话逐步覆盖全库。
- 备份导出改用只读快照，不再占用写队列、不再虚增 revision。
- 同一条记录的多张照片可用「上一张/下一张」连续翻看。
- 重构：编辑器的草稿持久化与录音逻辑拆为 `editorHooks`（useDraftPersist/useRecorder）；清理 Glass 从未生效的 `intensity` 参数。
- 流水线：iOS 启动冒烟对系统横幅遮挡做重试；Android 冒烟对慢冷启动与 dump 抖动做重试加宽。

## Build 57 — 纪念的礼物感

- 书架头部年龄行：「2 岁 3 个月 · 来到世界第 825 天」（填了生日才显示）；出生当天、第 100 天与每周岁当天出现印章式里程碑卡，点击直接记下这一天。
- 「那年今日」升级：全部往年同日的记录都排进横滑卡片条，每一段都能翻到，不再只取第一条。
- 年度册封面印章化：无照片的年份册以双线印章圆环加年份数字作纸封面；扉页印章画法抽成共享 `Stamp` 组件。
- 相册扉页寄语：每本相册可写一段最多 2000 字的话，与年度寄语共用「爸爸妈妈的话」纸卡；随本机备份保存。
- 纪念卡导出：阅读页一键把这条记录生成一张纸底衬线卡片图（照片 + 标题 + 正文 + 日期 + 装饰线），可保存、分享或打印；纯本机渲染，不需要网络，也不新增权限。
- AI 年度寄语起草：编辑年度寄语时可让 DeepSeek 依这一年的记录标题与「第一次」清单写一版草稿（纯文字、不含照片与精确位置，计一次写作额度），核对修改后才保存。
- 拍摄地点一键换地名：记录里有照片坐标且地点为空或仍是裸坐标时，可按需逆地理编码换成地名（Android 需允许定位，iOS 无需授权），确认后才写入。
- 编辑器「移除」素材增加确认弹窗，误触不再丢素材。
- 修复：Android 分享接收的 GPS 读取改用 EXIF 度分秒字符串解析，解决打包机的 Kotlin 编译失败；新原生代码先在本机对平台 SDK 验证编译。

## Build 56 — 数据底线与确定性缺陷修复

- 分享接收加固：一条坏掉的分享素材不再毒死整批——坏条目跳过并计数、批次之间互不影响；写库失败时收回已复制的文件，不再静默膨胀；无可保存内容的批次直接确认，不再每次启动重放。
- 分享草稿补齐拍摄信息：从微信等应用分享进来的照片自动读取 EXIF 拍摄时间与 GPS（Android 声明 `ACCESS_MEDIA_LOCATION` 保留相册照片定位），草稿自动落日期地点并开启按天分组建议，与编辑器导入一致。
- 按天分组两处修正：先写文字再导入多天照片时，标题正文只留在第一组；无拍摄时间的视频／录音跟随相邻照片归入同一天，同一场合不再被拆散。
- 编辑器修复：iOS 分组日期选择器不再一闪即关（滚轮加「日期选好了」确认）；录音入库或放弃后清理原始文件；保存忙碌时返回手势给出提示而非静默吞掉。
- AI 额度公平：上游失败或超时不再消耗当日额度；请求已结束时隐藏「重试原请求」，只留「重新生成」并说明只有重新生成才计入额度。
- 备份守护：书架在有记录且从未导出或超 30 天未导出时温和提醒（不弹窗）；备份页显示上次导出时间；本机保留最近 3 份可读文件名的备份、超出自动清理并支持删除；启动恢复推荐位只推荐完整校验通过的备份。
- 测试与流水线：Android 冒烟新增备份导出／删记录／恢复闭环，iOS 原生回归覆盖年度册寄语，新增 fixture 与模型形状对齐测试，APK 签名证书指纹钉进打包验证。
- 细节修正：异常兜底页深色模式可读；浅色辅助文字对比度提升至 4.5:1；月册／年度册／我的页去掉多余的顶部留白；书架、第一次、月册空态补 Ornament 收尾；分享扩展显示名改为「小美成长记」；阅读页正文可长按选择摘抄。

## Build 55 — 年度册与年度寄语

- 书架新增「年度册」区：每一年自动汇成一册，按年倒序，封面取当年第一张图，标注时光数与「第一次」数。
- 年度册内页：这一年的统计（时光、照片、影音、第一次、字数）、「这一年的第一次」列表、当年月册小册，以及「爸爸妈妈的话」年度寄语（最多 2000 字，随本机备份保存）。
- 旧版资料库与旧格式备份自动补齐年度册字段，打开与恢复不受影响。

## Build 54 — 全面精致化

- 衬线标题加克制字距，卡片阴影更轻更柔，纸张层叠感更细腻。
- 无照片的纸封面左侧带赤陶书脊细条；扉页印章改双线圆环，扉页收尾加细线菱点装饰。
- 阅读页日期配强调色小竖条，媒体间距统一；「删除记录」弱化为辅助色文字按钮；记录张数与「第一次」角标改为纸面描边样式，深色下不刺眼。
- 页面转场改为淡入淡出；进入与按压动画尊重系统减少动画设置，开启时全部取消。

## Build 53 — 书架动画

- 书架活了起来：月度册与专题册进场时按顺序交错淡入上移，「那年今日」卡片与「记一刻」悬浮按钮带进场动画。
- 书册与「记一刻」按下时改为弹簧缩放、松手回弹，取代单纯的透明度变化。
- 动画风格克制，暖纸手账调性不变；大字号与单栏布局下表现一致。

## Build 52 — 纪念品书架首页

- 首页改为纪念品书架：顶部扉页展示册名与统计，月度册与专题册以书脊式 Volume 排列。
- 「第一次」合集与「那年今日」卡片直接陈列在书架上，翻阅即达。
- 移除旧底部 tab，相册等入口整合进书架与各册内页。
- 「记一刻」悬浮按钮保留在书架右下角，随时开始写记录。

## Build 51 — 暖纸手账主题

- 全应用改用暖纸手账视觉：暖纸底色、手账质感卡片与按钮，取代液态玻璃风格。
- 记录、编辑与设置各页面统一新主题；浅色、深色与大字号规则不变。

## Build 50 — 修复分组与润色上限

- 修复「按事情分组」：此前分组请求只带空照片列表，一次请求都不发出并直接报「AI 分组不完整」；现在按整份草稿的照片分组，至少两张即可正常分批整理。
- 修复确认分组丢照片：关闭「按拍摄日期建议分组」后确认 AI 分组，此前只有第一组留在草稿里，现在每件事都写回草稿。
- 润色上限改由服务端按正文计算，标题长度不再误报；标题与正文合计过长时先在本机说明，不再落到笼统的「输入内容无效」。
- AI 图标的小圆点只在结果确实待查看时出现：收起面板后才返回的结果会亮起，打开面板即视为已读。
- 点「AI 生成」「润色我的文字」会切到写记录任务，生成完成后直接展示结果，不再提示「还有一份建议待确认」。
- 「重试原请求」「重新生成」只在本地校验通过后出现；生成时正文超出单次上限会明确说明只发送前 N 字，不再静默截断。
- 底部面板标记为读屏模态；本机备份与分享测试改用 POSIX 路径写法，Windows 下不再误报。
- 没有设备凭证时先收起 AI 面板再进入「AI 设置」，不再把面板留在设置页上层；iOS 原生回归测试同步改用新的 AI 图标与底部面板标识。

## Build 49 — AI 编辑底部面板与润色

- 编辑工具栏改为单个 AI 图标（图标 22、触控至少 44），打开共享主题的底部面板，不再常驻大面板；有结果待查看时图标显示小圆点，收起面板保留任务与结果。
- 「写记录」提供「AI 生成」与「润色我的文字」：润色只发送当前事情的标题和正文、不发送照片；正文一次最多 2000 字，超限明确提示且不截断；结果与原文对照，可全部采用或只采用标题、正文，采用前不覆盖原文；纯文字草稿也能润色，采用后直接写回正文并随保存与重启保留。
- 「整理照片」提供「按事情分组」，用于至少两张照片的新建草稿；分组预览可调整照片归属，摘要不写入正文、不自动触发写作。
- 本地 AI 任务与建议保存写作模式；重试按操作、模式、所属事情和输入区分，生成与润色不再串用结果，旧数据缺少模式时视为生成；输入变化后旧建议过期，不覆盖新内容。
- 首页「记一刻」改为小型悬浮按钮；多图记录以张数角标展示，与「第一次」合并显示。
- 服务端对润色正文超限返回明确的 POLISH_TOO_LONG 错误；模型继续固定 DeepSeek Flash High，提示词集中在服务端维护。

## 0.1.0 — 液态玻璃（构建 44）

- 首个 0.1.x 发行版；历史预发行标签与发布已全部清除。
- 全面改用液态玻璃视觉：静态氛围光斑背景上的半透明模糊卡片、输入框、按钮与底部导航；iOS 26 使用系统玻璃，其余平台用模糊视图并回退半透明。
- 记录卡片、「记一刻」浮动按钮、编辑保存栏与选材底栏均为玻璃材质；浅色、深色与大字号规则不变。

## Build 43 — 本机成长记录

- 独立本机记录、草稿、素材、相册和选择会话。
- Airbnb 温暖照片风，统一苔绿主题、直接选材和全屏编辑。
- 完整本机备份、恢复前备份、文件校验与失败回滚。
- 移除 Web 服务端、账号、上传和同步流程。
- 使用全新本机数据库；旧测试目录保持原样，不自动迁移或删除。

旧版本历史保留在 Git 提交记录中。
