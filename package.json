{
  "name": "xiaomei-growth-journal",
  "version": "0.1.0",
  "private": true,
  "description": "Android and iOS journal stored entirely on the device.",
  "scripts": {
    "postinstall": "npm --prefix mobile ci",
    "start": "npm --prefix mobile start",
    "android": "npm --prefix mobile run android",
    "ios": "npm --prefix mobile run ios",
    "typecheck": "npm --prefix mobile run typecheck",
    "lint": "npm --prefix mobile run lint",
    "test": "npm --prefix mobile test",
    "build:android": "npm --prefix mobile run build:android",
    "build:ios": "npm --prefix mobile run build:ios"
  }
}
